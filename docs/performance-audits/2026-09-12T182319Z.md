`2026-09-12T182319Z` (Linux x86_64, shared Emerald Rapids; **Preserve a
bounded fatal exit when diagnostic output cannot be written**).


A full shared filesystem exposed recursive fatal reporting in the retirement
census and differential harness. `os_file_write` validates a failed write;
`os_fail_va` previously printed through the same fatal writer, including when
stderr was also unwritable. That recursed until SIGSEGV instead of retaining
the intended unsuccessful exit. The original failed runs remain failures.

The formatted reporter now formats its message once and delegates to the raw
reporter. Raw diagnostics use the existing recoverable write attempt and exit
with status 1 even when no diagnostic bytes can be published. Successful
compilation and native backend selection are unchanged. This is a cold failure
path correctness repair, with no throughput or retirement-budget claim.

`os_tests` registers independent child observations for raw/formatted reports
with a working stderr, a closed stderr on desktop platforms, and `/dev/full`
on Linux. Each observation has a 30-second deadline and requires a normal exit
status of exactly 1. Working-stream controls also require the complete expected
message; failed streams must emit no captured bytes. No deadline is relaxed.
The explicit child environment preserves the parent snapshot while replacing
only the private test mode and worker count; the ordinary test command has no
new production option.

The original `os.c` plus the new test hook reproduced exits `1,1,-11,-11,-11,-11`
in mode order raw-live, formatted-live, raw-closed, formatted-closed, raw-full,
formatted-full. The fixed implementation produces `1,1,1,1,1,1`. Both compiler
images and their separate observations are retained as immutable evidence.

The same failure is reproduced without filling a filesystem: redirect the
TCC-built build driver's `native_retirement_census --self-test` stdout and
stderr to `/dev/full`. Original driver SHA-256
`c3df5eba73d726e5efba6bd80a47d1eacdc767516d8d05616e7d1f8226a8daa1`
terminated by SIGSEGV; repaired driver
`de40acaac047dfe0f32c9a6f11cc1290da3df6fb6eb8d7fb73e4f469620a23ca`
exited 1. With writable output the self-test passes. Both drivers use pinned
TinyCC `0fb54300b56512754221d80adda85ddb9815bceb`, not the failing packaged 0.9.27.

Debug full-suite validation passes 2,628,735 assertions in all 48 modules.
Release, ordinary/repeated self-host and platform checks are reported against
the published head in the PR. This repair does not resolve the remaining MIR
selection gaps or authorize direct-backend retirement.
