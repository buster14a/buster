`2026-09-12T172940Z` (Linux x86-64, shared Emerald Rapids; **X86 scalar MIR
vector arithmetic**).


This is a dependent implementation slice for #36, based on #493 head
`f94fb63900b27f6bb986dbb6e31fae42773dcbac` (tree
`57b099b242840b01a6531fc4f9105724cb849fd9`). It preserves main through #491.
It does not remove production direct dispatch or establish retirement acceptance.

The current-main/candidate census preceding this slice reconciles 74,304 common
rows and all their original bytes. #493 fixes 108 configurations, leaving 1,584
supported-native gaps across 27 whole fixtures. This slice addresses the x86
vector arithmetic rows exposed by `basic_c_vector.c` and
`basic_c_vector_lane_edges.c`; wide signatures and general assembly remain.

The implementation uses exact-width lane loads, existing integer/FP MIR rows,
explicit comparison-mask expansion and owned frame images for ZMM snapshots.
Native EVEX operations retain their existing path. Wide vector members now store
through their exact subobject address. No new opcode, direct-emitter invocation,
compiler dependency or replacement pipeline is added.

Registered tests preserve both whole fixtures and add scalar host observations
of 8/16/32/64-byte results, signed narrow arithmetic, unsigned masks, floating
arithmetic and adjacent bytes. Both C lowering forms, PIC settings, all native
targets and baseline/Haswell/Zen 5 x86 object profiles remain in the matrix.
Matching host CPU profiles execute; cross objects are not labelled native runs.
The new observer's ABI is explicitly void-pointer based in both translation
units and its arithmetic contains no vector extensions.

Failed development observations remain in the evidence: comparison temporaries
initially claimed the preceding CMP as their definition instead of SETCC; strict
MIR verification rejected those rows. Existing ZMM aggregate construction then
exposed the missing wide-member store. The first oracle expression combined
signed comparison masks with unsigned vectors, which the current C frontend
rejects even with an explicit vector bitcast. The final oracle tests the same
unsigned comparison in a separately typed output. The rejected original is
retained as an unsupported-reference observation, not renamed corpus support.
Nine earlier vector-statistic assertions implicitly depended on per-function
fallback. They now select NONE explicitly for those direct-emitter counters;
the added strict MIR matrix preserves whole-fixture object and semantic checks.
One host link omitted TMPDIR on the full workspace and failed; the corrected
Clang/GCC scalar observations both execute successfully.

The PR records immutable final-tree Debug/Release, corpus and platform results
as they finish. Shared-host development timings are not acceptance measurements.
#36 still needs an explicit maintainer budget and workload decision, authorized
exclusive 9700X execution, frozen external resources/recipes, all remaining MIR
implementations, then production dispatch removal, NONE transition, direct-only
code deletion and permanent retirement guards. No budget approval, merge or
umbrella closure is claimed here.
