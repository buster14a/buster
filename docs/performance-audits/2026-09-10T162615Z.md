`2026-09-10T162615Z` (Linux x86-64, Clang 18.1.3, QEMU AArch64; **Native MIR follow-up differential and matched throughput evidence**).

This records PR #393's compiler commit `7e8138345c6ddc43d3206e91808f5e4e1b09f34a`, tree
`737e3ac5947ffd9c786326a7db59acc05cc4a7d4` (identical local tree at `3c35cbbe`).
It advances GitHub #36; the native direct backend is still present and this is
not retirement acceptance. The [preceding audit](2026-09-10T160033Z.md) records
the implementation, published baseline census and earlier failed attempts.

## Matched compiler builds

Both source revisions were built with genuine Clang 18.1.3 and the repository's
TCC bootstrap, Release, non-unity, on the same host:

```sh
./build.sh generate --cc clang --linker DEFAULT -DBUSTER_UNITY_BUILD=OFF
./build.sh build --config Release -t ide
```

The separate baseline worktree is frozen at `d81149e8dc07621abd7a00de7e989dae7019a342`.
Its earlier unity-built binary was not reused for timing. Each resulting
compiler was copied to an immutable path before measurement. No build, test or
other benchmark from this task ran during the timing experiment. This shared
host does not provide a reserved physical core or a contention-free guarantee.

| Compiler | SHA-256 | Executable bytes |
| --- | --- | ---: |
| Baseline d81149e | `eb03617aed1bd0eddf3be9c2b7d06e30d34ccb46c81f13fa9e29fae0a777173c` | 40,526,024 |
| Candidate 7e813834 | `dfbca6dfb6053aa9e2bc8ccb14eb6ca606b4783f30d6b051243675ef234fbfdd` | 40,635,304 |

Build caches and compiler/input hashes are retained in the evidence directory.
Trailing whitespace is normalized in the copied caches and host-status text;
sample and argv records remain byte-exact.
The baseline-to-candidate comparison includes merged #362 and integrated #370
and #366; it must not be attributed solely to this PR's diff against 789b4c37.

## Throughput and memory

The unmodified native harness (`tools/throughput/throughput.c`) passed its
30-assertion self-test. Its default `ci` profile measured all six synthetic
workloads in all four allocator modes. The declared design is two rounds of
20 adjacent A/B pairs, two warmups per variant/case, seed 20260907 and
`--cpu auto`. All 1,920 measured compiler invocations were retained.

```sh
throughput run --baseline /absolute/baseline/ide --candidate /absolute/candidate/ide --output /new/output --baseline-id d81149e8dc07621abd7a00de7e989dae7019a342 --candidate-id 7e8138345c6ddc43d3206e91808f5e4e1b09f34a --cpu auto
```

Result: **0 confirmed regressions, 18 inconclusive cases**, guard enabled,
exit 0. The existing guard tests wall time at 15% plus 2 ms and peak RSS at
20% plus 16 MiB, requiring significance in both rounds. Inconclusive does not
mean equivalent. No samples were discarded or retried to improve this result.
See the [full table](2026-09-10T162615Z-evidence/summary.md),
[raw samples](2026-09-10T162615Z-evidence/samples.csv),
[statistical results](2026-09-10T162615Z-evidence/summary.json), and
[exact commands](2026-09-10T162615Z-evidence/commands.jsonl).

Hosted CI's independent comparison against base 789b4c37 also completed with
**0 confirmed regressions and 16 inconclusive cases** on this exact head
([job 102951138788](https://github.com/buster14a/buster/actions/runs/34500833547/job/102951138788)).

Generated object sizes are unchanged in all 24 synthetic workload/mode series.
This compares output file sizes, not extracted text-section sizes or runtime.

This is compiler throughput/peak-RSS evidence. It does not establish a final
runtime budget for generated programs or acceptance of all target ABIs.

## Correctness and current coverage

The immutable candidate passed the native differential runner's **6,912
configuration/case checks**: 16 cases times 432 configurations, zero failures,
strict verification enabled. Exact invocation:

```sh
./build.sh test_differential --ide /absolute/immutable/candidate/ide --cc clang --out /new/differential-output --minimize 0
```

Both independent Clang O0/O2 controls must agree before each successful-source
case becomes an oracle. This run is **unsanitized**; it does not replace the
sanitizer-oracle run blocked by LeakSanitizer's inability to inspect process
tasks in this environment. The prior timeout failures remain in their original
evidence. See the [complete case results](2026-09-10T162615Z-evidence/differential.log)
and [manifest](2026-09-10T162615Z-evidence/differential-manifest.txt).

Other exact-compiler validation: **916,629/916,629 assertions, 46/46 modules**;
all self-host stages completed with a **38,657,152-byte fixed point**; all
**24 mode-matrix legs** passed (8 executions and 16 object/link oracles).
The three integer/vector edge fixtures passed **12/12 QEMU executions** across
all four allocator modes. Clang/GCC O0/O2 controls passed 8/8 native executions.
The earlier stale-stage self-host failure and its unconfirmed cause are
preserved in the preceding audit.

A refreshed census uses the same 296 frozen fixtures on all six targets, with
two workers and 30-second deadlines. There are still **28 gained successful
compilations and zero lost baseline successes**, and no losses versus a03e9c83.
The new integer lowering removes one additional fallback function each on
AArch64 Linux/macOS. Some functions reveal their next unsupported opcode after
their first gap is fixed; counts are first-failure counts, not all-operation inventories.

| Target | Successful compiles | Fallback functions | Fixtures with fallback |
| --- | ---: | ---: | ---: |
| x86-64 Linux | 275 | 191 | 41 |
| AArch64 Linux | 273 | 30 | 8 |
| x86-64 Windows | 272 | 184 | 39 |
| AArch64 Windows | 272 | 1,324 | 266 |
| x86-64 macOS | 273 | 192 | 42 |
| AArch64 macOS | 274 | 62 | 22 |

[Current per-fixture telemetry](2026-09-10T162615Z-evidence/coverage.jsonl) retains compile
status, counters and fallback rows; failed compiles are not treated as zero
fallback successes. AArch64 Windows remains deliberately excluded pending
correct machine-frame unwind and variadic ABI handling. Wide arithmetic,
128-bit atomics, general inline assembly and public AAPCS64 list interoperability
remain tracked prerequisites. No production fallback or previously supported
program was removed to improve the counts.
