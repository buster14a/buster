`2026-09-11T155753Z` (Linux x86-64, Intel i7-1185G7, Clang 22.1.8; **Remove
unused machine form and expansion identities**).


## Scope and provenance

Baseline main: `5324b7d077d5727a23dd680c0a3efb778a55ff7f`.
Measured code/test commit: `603f4bb19a30a604be54cc19f3df47f716b7a5e0`
([PR #425](https://github.com/buster14a/buster/pull/425)). This audit is a
subsequent evidence-only change. Both compilers were built with the normal
TCC-bootstrap local Release workflow, Clang 22.1.8, O3, native CPU selection,
debug information and frame pointers. Their exact binary hashes and commands
are in the [metadata](evidence/2026-09-11T155753Z/metadata.json) and
[command log](evidence/2026-09-11T155753Z/commands.jsonl).

The patch removes only the unused descriptor form/expansion fields, their
initializers/accessors, three unused enums and one unused macro. Explicit
padding keeps every retained descriptor offset. The 96-byte descriptor,
24-byte machine instruction and 16-byte published row do not shrink. No new
production allocation, projection, pass or encoding policy is introduced.
The existing recipe registry remains the emission-policy identity authority.
The ten additional machine assertions and ownership documentation explain
category/index distinctions and effects required before metadata consumption.
This is partial progress on #45, not completion of the full dormant census.

## Diagnostic paired measurement

The existing native throughput harness's self-tests passed, including 602
integration assertions. After the local build/self-host/mode workflows ended,
run the uninstrumented compilers on six frozen smoke workloads, four allocator
modes, two rounds, eight pairs per round and one warmup per compiler/job:

```sh
./build.sh bench_throughput self-test
./build.sh bench_throughput run --baseline /absolute/baseline-ide --candidate /absolute/candidate-ide --output ../throughput45 --profile smoke --mode all --pairs 8 --warmups 1 --no-guard --require-identical-output --baseline-id 5324b7d077d5727a23dd680c0a3efb778a55ff7f --candidate-id 603f4bb19a30a604be54cc19f3df47f716b7a5e0
./build.sh bench_throughput compare --output ../throughput45
```

All 768 process samples (384 pairs, 24 workload/mode series) are retained in
[samples.csv](evidence/2026-09-11T155753Z/samples.csv). All compared output
hashes match. See the complete [timing/RSS table](evidence/2026-09-11T155753Z/summary.md)
and [statistical replay](evidence/2026-09-11T155753Z/summary.json).

This host reports an Intel i7-1185G7, eight logical CPUs with SMT enabled.
Affinity was not pinned; the desktop was active. No other task-owned build,
test or benchmark ran during the timed samples. PMU/allocation replays were
not requested. This is not Zen 5/Zen 4 evidence. The smoke scale, low pair
count and disabled guard make this diagnostic only: the printed zero
regressions/inconclusive counts do not establish equivalence or acceptance.
No compiler or generated-program speedup is claimed.

FAST median wall milliseconds, baseline/candidate: startup 21.745/20.991,
large function 22.371/22.511, many functions 23.368/23.350, symbols
24.602/24.223, control flow 29.219/28.454, pressure 37.447/36.648.
Across all series, median peak RSS **increased by 0.496–0.791 MiB**.
That observation is retained; this experiment does not isolate its cause.
Static table-byte savings are zero, and the change adds no dynamic allocation.
A statistically accepted whole-compiler nonregression claim still needs the
ordinary broader throughput gate; this small experiment is not a substitute.

## Correctness and remaining validation

The unchanged baseline passed ordinary TCC-bootstrap self-hosting, including
a byte-identical 38,780,008-byte FAST pair and MIR_STACK/NONE gates. Its
Release binary passed 1,483,095 assertions / 46 modules on the unchanged
fixtures. The candidate passed 1,483,105 assertions / 46 modules, ordinary
self-hosting with a byte-identical 38,779,592-byte pair, MIR_STACK/NONE gates,
and all 24 mode-matrix legs (8 executed, 16 oracle-checked, zero failures).
Commands were `./build.sh build --config Release -t test_all`,
`./build.sh test_self_host --config Release`, and
`./build.sh test_mode_matrix --config Release`.

The candidate's changed self-host input is not the baseline input; self-host
stage timings are correctness evidence, not a paired speed comparison.
A controlled repeated Clang compiler-build-time measurement was not collected.
At audit creation the separate local Debug ASan/UBSan build and hosted checks
are pending, not passed. Exact-head final results belong in the PR; the audit
will not be rewritten to substitute a later revision's results. Full issue
acceptance and #36/#37 work are not claimed here. `git diff --check` passes.
