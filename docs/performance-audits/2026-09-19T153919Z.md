`2026-09-19T153919Z` (Linux x86_64, Ryzen 7 9700X; **sparse x86 LOCAL sizing
has no measured stage-1 throughput gain**).

## Result

The sparse LOCAL walk preserves output but does not reduce measured DRAM fills or compile cycles on this workload. The cycle point estimate is slightly worse. Keep the performance claim open; this measurement does not justify claiming a throughput win.

Independent item 2 of #791, PR #854. All 22 warm/measured outputs and both full survey outputs were byte-identical. Every `IR_FAST`, `IR_LOCAL_PROMOTION`, `IR_LOCAL_PROMOTION_WORK` and `CODEGEN` summary line matched the baseline exactly.

| Metric | Base median | Candidate median | Candidate delta |
|---|---:|---:|---:|
| Wall time (ns) | 1,081,401,880.5 | 1,083,583,040.5 | +0.2017% |
| User cycles | 5,385,347,394.0 | 5,396,949,417.0 | +0.2154% |
| User instructions | 14,382,730,894.0 | 14,380,288,125.0 | -0.0170% |
| DRAM/IO fills | 12,639,881.5 | 12,683,520.0 | +0.3452% |

## Identities and method

- Frozen input and baseline compiler source: `2a9693b23b869461aab68835a1221dba2d8c3f1f`.
- Measured candidate source: `703d2c2d22cd15aa19ea1b2065234404f6fd9018`.
- Base trusted Clang Release compiler SHA-256: `b315e6f10129c01b05463a0eef8e91f39d3af8a97b566327e5896c20bcc73386`.
- Candidate trusted Clang Release compiler SHA-256: `b8c1009c9ff80c135229af4cc4eeda7d34d99b709d26db60953267d54f80ffd8`.
- Common generated executable SHA-256: `96c501c5c0f4ad74171635dfa15f755f39712ee5b7a8e0811869f4616c0f0965`.
- Host: AMD Ryzen 7 9700X (`benchpress`), Linux x86-64, Clang 22.1.8, canonical TCC 0.9.28rc bootstrap; complete identity in [host.txt](evidence/2026-09-19T153919Z/host.txt).

The user authorized direct Tailscale SSH access to the dedicated host. Runs held `~/bench/host.lock`, with no concurrent build or measurement, and pinned each compile to CPU 2. Both independently built compilers consumed the **same baseline checkout and generated headers**, with the same command:

```sh
COMPILER cc -Isrc -Ibuild/generated -DBUSTER_UNITY_BUILD=1 \
  -DBUSTER_INCLUDE_TESTS=0 -g -v src/buster/apps/ide/ide.c -lm \
  -o build/issue791-stage1
```

One unmeasured warm-up per compiler preceded ten paired measurements. Each pair reversed order (AB, BA, AB, BA, ...). `perf stat -x,` counted `{cycles:u,instructions:u,ls_any_fills_from_sys.dram_io_all:u}` together. Wall time used a monotonic nanosecond clock around that same perf invocation, including its common process overhead. These are medians of ten observations per leg, not a statistical equivalence claim. This focused diagnostic is not the separate native-retirement acceptance run.

Both legs also completed `tools/cache_miss_survey.py --dwarf --repeat 3 --cpu 2 --binary COMPILER`, from the frozen baseline checkout. The full reports retain per-symbol/source-line attribution. Those samples have skid and are used for attribution; the paired counters above own the comparison.

## Correctness and integration

The measured candidate passed `./build.sh test_self_host --config Release` and `BUSTER_TEST_JOBS=4 ./build.sh build --config Release -t test_all` on the 9700X:

```text
SELF_HOST fixed_point bytes=42557224 stage1=build/self-host/Release/ide-stage1 stage2=build/self-host/Release/ide-stage2
[2763138/2763138] Unit tests
[53/53] Module tests
```

`./build.sh test_mode_matrix --config Release` also passed all 24 legs on this host.

The later integration commit `d415268719e02e6c3404eed1ae516fe1fed7f47a` includes current main `e013ef03b560677b1a6b47590e9eb05171b9b34d`. Its additional upstream change only repairs OS test readiness; it changes no production compiler source. Fresh self-host/full-suite validation and PR CI cover that integration separately. The measurement identities above remain explicit rather than being relabeled as later revisions.

The local Intel workstation reproduced the baseline fixed point, but its full suite has nine existing Windows/Wine mixed-vector failures on unchanged main. The completed local promotion-candidate run reproduced those nine failures. The dedicated 9700X full suites pass; no local failure is represented as green.

## Retained evidence

- [Every paired sample, binary hashes and exact command](evidence/2026-09-19T153919Z/paired.json).
- [Identical compiler summary](evidence/2026-09-19T153919Z/compiler-summary.txt).
- [Baseline cache-miss/DWARF survey](evidence/2026-09-19T153919Z/base-cache-miss-survey.txt).
- [Candidate cache-miss/DWARF survey](evidence/2026-09-19T153919Z/candidate-cache-miss-survey.txt).

Raw perf captures and full build/test logs are retained under `/home/david/bench/issue791-20260919/evidence` on the authorized host. No old audit or closed index was edited.
