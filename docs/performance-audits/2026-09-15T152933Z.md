`2026-09-15T152933Z` (Linux x86_64, shared 4-vCPU container (not a dedicated
bench host); **Event-driven MIR debug-location recording: stage-1 default
self-compile 9.70 s -> 3.11 s user CPU**).

## Workload and setup

Stage-1 unity self-compile, the workload
[#646](https://github.com/buster14a/buster/issues/646) reports:

```sh
./build/Release/ide cc -Isrc -Ibuild/generated -DBUSTER_UNITY_BUILD=1 \
    -DBUSTER_INCLUDE_TESTS=0 src/buster/apps/ide/ide.c -lm -o ide-stage1
```

Both compilers are Clang-built Release `ide` binaries from the same tree, so
the only difference between them is the change under test. Runs are
interleaved (before/after, default/`-g0`, six rounds) and reported as user
CPU time, because wall time on this shared container swings by seconds
between identical runs. **This is not the 9700X bench host the issue's
completion criteria name**, and `perf` is unavailable here, so there is no
sample profile in this audit: the A/B below and the stage bypass are the
measurements.

## Paired A/B

| Compiler | Mode | min (s) | median (s) |
|---|---|---|---|
| before | default | 9.695 | 9.964 |
| before | `-g0` | 2.715 | 2.840 |
| after | default | 3.111 | 3.170 |
| after | `-g0` | 2.752 | 2.841 |

Default-mode user CPU falls 3.12x. Debug info cost 3.57x `-g0` before and
1.13x after (1.12x on medians). The issue asks for within 10%; the remaining
13% is accounted for below and is not met on this host.

## Where the remaining cost is

An env-gated bypass compiled into a throwaway binary (not committed) staged
the recorder. Same workload, user CPU, best of two:

| Stage | s | delta |
|---|---|---|
| `-g0` | 2.86 | — |
| debug info on, recording skipped entirely | 2.99 | 0.13 (debug value tables, DWARF emission) |
| + per-function index built | 3.06 | 0.07 |
| + spans resolved and reference timelines replayed | 3.32 | 0.26 |
| + seeds emitted | 3.33 | 0.01 |

That bypass predates the two cost fixes that followed it — skipping runs of
block starts that cannot change a tracked register's state, and indexing only
the virtual registers a debug value names — which together removed about half
of the replay column. The residue is the per-function index (O(rows + edits +
marks), paid once per function whatever the debug value count) and the
replays themselves.

## Output equivalence

Recording is a rewrite of a byte-identical contract, so equivalence is the
result, not a side note. The before and after compilers were each asked for
`-g -c` objects of the stage-1 translation unit (about 1.5M lines after
preprocessing) in every allocator mode:

| Allocator | Object | Result |
|---|---|---|
| default (`fast`) | 46,629,496 bytes | identical |
| `-fregister-allocator=quality` | 46,376,328 bytes | identical |
| `-fregister-allocator=mir-stack` | 57,890,976 bytes | identical |

`./build.sh test_self_host --config Release` keeps the fixed point
(`SELF_HOST fixed_point bytes=40381096`), `test_all` passes in Release and
Debug, and `test_mode_matrix --config Release` passes all 24 legs.

## Seed storage

Seed arrays now grow with what recording emits instead of reserving
`debug values x (rows + 1)`. On this workload that bound reserved 357M seeds
(20 GB) to emit 167k. The regression fixture records a function whose bound
would be 41.7 GiB — past the 32 GiB translation-unit reservation that used to
abort the compile — and it now completes inside a normal arena.

## Change-point dedupe

The timelines were not as sparse as the design intends. A row's own sample
supersedes the steady state the previous row published for that same row, and
the push compared the incoming selection against the entry it was about to
overwrite instead of the one that would end up preceding it. A row whose
sample differs from its own steady state therefore kept an entry, and a
function of such rows kept one per row, all carrying the same location. Every
value naming that register then walked all of them: the whole-function replay
again, once per value.

Measured on a function of 40,000 rows and 20,000 values naming one register
(`codegen_test_machine_debug_seed_sizing`), replay iterations were
800,020,000 -- exactly `values x (rows + 1)` -- across a timeline of 40,000
entries that describe one location. With the comparison made against the
surviving neighbour the timeline is 1 entry and the emitted seeds are
unchanged.

| Build | `codegen_tests` | whole Debug suite |
|---|---|---|
| before this change | 33.258 s | 2 m 27.5 s |
| after | 0.282 s | 1 m 51.6 s |

The Debug (`BUSTER_OPTIMIZE=0`, non-unity) suite is what the mobile lanes run
under a per-payload deadline, so this was a CI-visible regression, not only a
pathological-shape one. On the stage-1 workload above the same fix is worth
about 1.5% (2.981 s -> 2.936 s user CPU, best of three): real functions rarely
name one register from many values across many rows.

`codegen_test_machine_debug_widest_timeline` reports the widest timeline a
recording builds, so the fixture asserts the sparsity structurally rather than
by wall clock. Reverting the dedupe fails that assertion.

Byte-identical `-g -c` objects of the stage-1 translation unit, base compiler
against this one over the same source tree: default 46,828,240 bytes,
`quality` 46,587,672 bytes, `mir-stack` 58,161,432 bytes, identical in all
three. `test_all` (Release and Debug), `test_self_host`
(`fixed_point bytes=40537688`) and `test_mode_matrix` (24 legs) pass.
