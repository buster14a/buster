`2026-09-09T011927Z` (Linux x86-64, Intel Core i7-1185G7, Clang 22.1.8;
**Recover per-site allocation census through the existing optional
observer**).

## Recovery scope

Stacked on the compiler-throughput recovery at
`e33d4f67d078f7998018198d0c66795dbd879a00`. Its compiler source matches
`a4cdf1595994ca5247919613e0494e8f6ae0abd2`, based on main
`c803c6e556e1f260ece419e85424faff01c29687`. The verified allocation transport
SHA-256 is `9793a8a47882ff875a1966ee6f2919f73fc8ca88ef07d2a5ea337413c9753271`,
from commit `7b0e23ea24cc3233a91a67386122f85fdb6b1f7b`. It is the related
source variant associated with [Optimize Compiler Allocations](https://chatgpt.com/c/6a9f2cc9-f530-83eb-b0ff-8dc0f798b69f);
the original abbreviated conversation commits remain unavailable.

The existing default-off `BUSTER_BENCH_ALLOCATIONS` authority now records
textual call sites, alignment advancement, zeroed-API requested/actually-cleared
bytes, small/empty/maximum requests and separate OS request/failure traffic.
The two stable source-metrics fields retain their calling-thread pre-formatting
snapshot. The optional exit census includes worker and cleanup traffic and is
deliberately a different observation boundary.

The recovered reporter needed several integration changes: one recorder instead
of a second switch, textual identities across translation units, independent
OS-kind reconciliation, no counter reset on reporting, a verified no-live-worker
completion boundary, serialized complete epochs and direct native error writes.
The original generic file-write failure path could format diagnostics after
thread-context cleanup; the new reporter exits on write failure without asking
for scratch storage. The V2 reader rejects incomplete/duplicate/inconsistent
records and atomically publishes JSON/CSV with the exact raw-log hash.

The old Python compiler runner and native launcher duplicate the throughput
infrastructure and are superseded. Their macro/aggregate workload generators and
page-fault/context-switch diagnostics are preserved as actionable follow-ups
[#346](https://github.com/buster14a/buster/issues/346) and
[#345](https://github.com/buster14a/buster/issues/345), respectively.
The ABI and member-search scratch changes in that transport belong to their
separate recoveries, not this change. See [allocation-census.md](../allocation-census.md)
for all counter and lifecycle limits.

## Validation record

The fresh canonical self-host gate passed before census source edits on
`1fe594b85d4fac02f624aafc52d6c75de6355260`. Its command time was 91.622 seconds
under concurrent host activity, recorded only as a correctness run.
The disabled raw/zeroed inline allocation bodies match the prior preprocessed
bodies after normalizing only assertion diagnostic paths/source lines.
The offline reader passes 30 regression tests. A strict ASan+UBSan real compiler
probe emits one complete epoch with 466 sites and all five independently
reconciled kinds; a full-device stderr probe exits 1 without formatted recursion.

The intermediate imported-main baseline at
`c4682875a56367d4ea8547d85177c7b7b69b619d` also passed the canonical self-host
gate. Its trusted Clang binary SHA-256 is
`7bc8810bc90210461db903601f72215e142f48a5d6cceb24e9c8ae121964d366`.
The 1,407.775-second command was a contended correctness run, not a performance
comparison. Two independent source reviews found no remaining material issue.

This draft retains the following pending gates explicitly: a fresh final-base
self-host baseline at `a4cdf159`, the census Release/full-suite and canonical
self-host checks, strict ASan+UBSan full-suite validation with report emission
enabled, and parsing a real multi-epoch worker report through final completion.
The earlier strict full-suite attempt was interrupted by the user before it
finished; it is not counted as passing. The single-epoch compile probe does not
substitute for worker lifecycle validation. Cross-platform execution remains
subject to hosted checks. No timing, RSS, allocation improvement or speedup
claim is made by this diagnostic tooling.
