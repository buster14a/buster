`2026-09-09T004819Z` (Linux x86_64, Intel Core i7-1185G7; AArch64 under QEMU;
**Canonical integer views for atomic aggregate exchange and compare-exchange
(#195)**).

Recovery source: [Tackle Problems 190 195](https://chatgpt.com/c/6a9eb3a5-2aec-83eb-b210-bdf7794ea878),
with the prepared fixture in
[the saved workflow](https://github.com/buster14a/buster/blob/7ed1678d1bf8df9aae34deebf86d2a021088a8b4/.github/workflows/agent-export-190-195.yml).
The isolated base is `9f5616613b4c1f1af4b4071a2cbe987db20490b8`.

The prepared assertion edit targets a removed backend. In the current compiler,
aggregate compare-exchange also conflicts with canonical IR's integer/pointer
contract and the frontend's direct integer equality on record operands. The
saved regression is recovered, but that stale one-line production edit is not
applied. On the current baseline frontend/IR/codegen, the recovered fixture is
rejected with `C code generation failed with error 2`; Clang with `-mcx16`
compiles and executes it successfully.

The frontend now converts aggregate C11 exchange/CAS into the existing integer
operations. `c_ir_atomic_aggregate_bits_place` retains the atomic/volatile
qualification while casting the address to the same-width integer view.
`c_ir_atomic_aggregate_bits_value` uses private scalar storage for the value
conversion; it zeroes the whole promoted slot before writing a smaller record.
Observed bits are converted back for returned records and expected-value
writeback. The comparison itself is over integer representations. Existing
aggregate load/store operations, memory orders, supported-width policy, IR
validators and native integer atomic emitters remain unchanged. GNU `_n`
builtins still reject records, matching Clang.

The recovered fixture covers two-long-long, four-int and pointer-plus-counter
16-byte records; strong CAS, stale strong CAS and expected writeback, bounded
weak CAS, load/store/exchange, and size/alignment. Added controls exercise a
three-byte record promoted to four bytes, a volatile record with six bytes of
members, and a union. Raw IR validation covers the default frontend SSA path
and its memory-form reference; exchange/CAS result types are ordinary integers
of the promoted width.

Validation:

- The unchanged base passed the complete Release self-host gate before this
  recovery series: a 39,903,752-byte fixed point, plus supplemental stages.
- Candidate Release passes 901,476/901,476 assertions and 44/44 modules.
- Candidate strict-sanitizer Debug passes 897,197/897,197 assertions and 44/44
  modules, with no ASan or UBSan diagnostics.
- Complete candidate Release self-host passes: a byte-identical 39,914,456-byte
  stage1/stage2 fixed point, stage2 benchmarks, MIR-stack and canonical stages.
- The target/allocator matrix passes 24/24 legs: eight execution legs and 16
  object/disassembly oracle legs, with no failures.
- The final fixture executes successfully in all 16 combinations of x86-64
  and AArch64, `none`/`mir-stack`/`fast`/`quality`, and frontend SSA on/off.
  Clang's independent x86-64 reference also exits zero.
- Buster and Clang both reject the GNU `_n` record negative control.

AArch64's 16-byte support predates this slice: the current fixture guard already
names both architectures, the aggregate access policy permits 16 bytes on
AArch64, and its canonical integer emitter uses exclusive-pair operations.
All eight AArch64 fixture legs execute the wide cases; they are not merely
small-width coverage. The stale x86-only statements in the guide and fixture
comment are corrected to match these facts.

No compilation-throughput or generated-program speedup is claimed. The existing
historical `2026-08-24T131821Z` audit-format failure remains unchanged.
