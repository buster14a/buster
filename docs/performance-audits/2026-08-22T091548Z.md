`2026-08-22T091548Z` (Linux x86_64, Zen 4 7940HS; **take the three cache-miss
leads: the promoted-member memset, the selector's per-value prepass, and the
linker's image copy**). The follow-up to `2026-08-22T084855Z`, which measured
the stage-1 miss budget and ranked what owns it.  All three leads paid, but
not in the column the survey ranked them by, and that mismatch is the
transferable part of this entry.

**What landed, in the order it was measured.**  Each is its own commit with
its own paired A/B against the binary immediately before it: eleven (nine for
the last) order-alternated CPU-pinned runs of the clang-built Release `ide`
compiling the unity `ide.c`, outputs compared byte for byte on every pair,
medians of the per-pair differences.

*The promoted-member searches stop clearing a type-indexed table.*
`c_ir_emit_field_place_from_value` and `c_ir_promoted_member_path` each
answered "already queued?" with a `bool` array indexed by type id, allocated
from the temporal arena and `memset` per call over the whole program type
table.  Probe counters put the first at **72.167 calls with capacities summing
to 1.257.994.291** — a mean of 17.432 entries, **1,26 GB zeroed per compile**
— to serve work lists that hold a handful of members.  The queue is the
visited set: a linear scan answers the same question, and it is the last term
of the push condition so a named field never runs it.

    instructions           -45.691.753  (-0,4466%)   11/11
    L1d load misses        -16.409.402  (-13,2721%)  11/11
    cycles                 -60.461.703  (-1,5356%)    9/11
    fills from DRAM/IO         +34.634  (+0,4786%)    4/11

*The x86 selector's local-promotion prepass is filled from the rows.*  It
opened with a loop over every value that read `values[i].definition` and
followed it into `instructions[]` to ask whether the value was defined by a
`LOCAL` row — a random 64-byte fetch for two fields, and
`machine_x86_64.c:4258` was the single hottest line in the survey at 6,17% of
the compile's DRAM fills. Four `memset`s and one ascending scan of the
instruction array replace it; the value's definition still has to point back
at the row, which keeps the selected set identical.

    instructions            -8.185.686  (-0,0804%)  11/11
    fills from DRAM/IO        -149.830  (-2,0944%)   8/11
    cycles                  -5.183.353  (-0,1341%)   7/11
    L1d load misses            -31.048  (-0,0291%)   6/11

*The ELF writer grows its image instead of copying it.*
`link_elf_section_table_append` allocated a second buffer and `memcpy`d the
finished 35 MB executable into it to append the debug sections and the section
table.  The image is the arena's last allocation at all three writers that
reach it and the layout only ever grows, so taking the tail alone leaves the
image where it is; the copying form stays behind an on-top test.

    fills from DRAM/IO        -284.631  (-3,8742%)  9/9
    instructions            -3.408.138  (-0,0335%)  9/9
    cycles                 -10.287.883  (-0,2535%)  5/9
    L1d load misses         -1.008.168  (-0,9241%)  5/9

**Together**, against the tree the survey measured, eleven pairs, outputs
byte-identical throughout:

    instructions           -57.293.195  (-0,5601%)   11/11
    L1d load misses        -17.185.124  (-13,9702%)  11/11
    fills from DRAM/IO        -351.411  (-5,0272%)   10/11
    cycles                 -43.754.964  (-1,0981%)    8/11

The budget re-measured on the changed tree agrees and says where it went: L1d
load misses `123.986.598 -> 105.776.514` (12,1 -> 10,4 per kinstr, 3,34% ->
2,91% of loads), IPC `2,59 -> 2,63`, and the DRAM latency upper bound `46,0%
-> 41,3%` of the run.  `__memset_avx512_unaligned_erms` falls from 9,37% to
2,42% of L1d leaves, `c_ir_lower_frame_fallback` — which was charged the
field-place zeroing through its libc leaf — from 9,25% to 2,95%, and
`c_ir_emit_field_place_from_value` itself from 2,28% to 1,48%.

**The lesson is which column moved.**  The survey ranked by DRAM fills,
because those are the expensive misses, and named the selector's prepass the
largest lead at 23,58% of them.  That lead returned `-2,09%` of DRAM fills:
real, and an order of magnitude below its share, because the instruction rows
it stopped fetching randomly are still fetched by the walk immediately below
it — only the order changed, and it is still the largest entry on every miss
event (20,07% of DRAM fills after).  The lead that actually paid had a DRAM
share of `0,22%`: a per-call `memset` over a temporal-arena block lives
entirely in L1 and L2, so it never appeared in the DRAM ranking at all, and
removing it returned 14% of every L1d miss in the compile and `-0,45%` of its
instructions. **A miss share says where a line's traffic goes, not how much of
it would disappear if the line did.**  Whether the bytes are touched at all,
and whether something else is about to touch the same bytes anyway, is the
separate question that decides the return; only the linker copy — 35 MB that
nothing needed — answered both the same way.

Gates: `test_self_host` reaches its byte-identical fixed point (`SELF_HOST
deterministic bytes=35392816`) and `test_all_combinations_ci` is green
locally, which is Linux only; Windows and aarch64 stay remote-pending.

Left untaken: `machine_select_canonical_function_x86_64` still holds 20,07% of
DRAM fills and 14,25% of L1d misses, and its four per-value arrays are still
four streams — interleaving them into one row per value is the next step
there, and it touches every consumer.  The preprocessor's token
materialization (`c_source.c:6391`, 5,50% of DRAM fills) and the two linker
section-data copies are unchanged; the first wants the lexer writing into the
final array, the other two are close to compulsory.
