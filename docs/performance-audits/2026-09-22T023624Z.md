`2026-09-22T023624Z` (Linux x86_64, shared execution container; **Desktop
phase observer control; no compiler performance acceptance**).

## Scope and decision

#892 diagnostic instrumentation only. No compiler optimization, scheduling
change, acceptance-threshold change, or speedup claim. Current main base:
`f5ce6cdc10e0e9f49b8698a82944daf4688c0f2e`, tree
`a1c25d897665dbdff356c1744208b98fbdb653eb`.

The local control runs a Python no-op through the actual native observer. Its
outer Python timer includes native driver startup, two atomic publications,
fixture directory setup and result parsing. Subtracting only the native
observer's immediate child interval therefore gives a conservative enclosing
control bound, not a paired whole-matrix overhead estimate.

Five retained samples from the first complete native-plan fixture pass, in
microseconds (`child`, `enclosing`, `observer upper bound`):

| Sample | Child | Enclosing | Upper bound |
|---:|---:|---:|---:|
| 1 | 11588 | 13885 | 2297 |
| 2 | 12059 | 13809 | 1750 |
| 3 | 11113 | 12597 | 1484 |
| 4 | 9606 | 12449 | 2843 |
| 5 | 10453 | 12471 | 2018 |

Observed bound: 1.484–2.843 ms per command on this shared execution container.
This supports screening the observer in hosted CI. It is not evidence of
Windows/macOS cost, compiler throughput, or current-main critical paths.
The hosted workflow retains the same five-sample control for each release
lane; every failed/incomplete attempt must remain visible. Instrumentation is
unset for native-retirement/ordinary compiler performance acceptance.

## Reproduction and provenance

`python3 tools/ci_matrix_phases_test.py -v` builds a temporary driver containing
the actual observation code plus a policy-export test seam. It changes only
the coverage self-test dispatch in that temporary copy. The CLI observation
worker and all production phase/policy serialization functions remain intact.
The compiler command uses `-Isrc -I. -fwrapv -fno-strict-aliasing -funsigned-char`
with the generated temporary C source and output path. This is a driver
control, not a trusted Clang-built compiler benchmark.

Host: Linux x86_64. GCC 13.3.0.

Observation source SHA-256 at recording:

- `tools/matrix_phase.c`: `a5e48f0584ee3a868625d5485972029d19d1773ea8058fe3539a9e810869f293`
- `tools/ci_matrix_phases.py`: `8bb9dcf25084b4f322b37452749d8e4e4f7383d88972fb319925e4e48c4c2fa0`
- `tools/ci_matrix_phases_test.py`: `4d998ed3a1b3a50af21e5b1cb68da5b04aaa2968fac2a5139a0f41cc1a63efaa`

## Required follow-through

A complete current-main desktop run and exact job inventory are still needed
for #892's six-lane ranking, measured overhead disposition and exactly one
successor/no-change choice. #891's historical slow Intel observation remains
`timestamp-invalid`; this control does not reclassify it. See
[the phase contract](../ci-matrix-phases.md) for the fixed-duration prediction
model and its limits. No historical audit or closed index was rewritten.
