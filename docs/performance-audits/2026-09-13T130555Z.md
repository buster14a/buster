`2026-09-13T130555Z` (GitHub Actions Ubuntu 26.04 x86-64, 4-vCPU AMD EPYC 7763
KVM, Callgrind Ir, explicit znver3 target; **canonical validation is 2.9% of
the frozen unity compile; measured checks are proportionate and no semantic
change is justified**).

Issue #564's 8.6% was not reproduced. On the frozen current source,
`ir_validate_canonical_module` is **472,211,973 Callgrind Ir, 2.860%** of a
**16,513,652,317-Ir** Clang-produced unity self-compile. The validation call is
inside the driver's first canonical preparation. That preparation is
1,246,700,050 Ir (7.550%); the later codegen entry calls preparation again for
316,815 Ir but does not call the validator. These are guest instruction
references, not hardware retired instructions, elapsed time, or a Zen 5 result.

The disposition is **no compiler change**. The largest attributable family is
value/type/label-provenance validation at 168.133 M Ir (35.61% of validation,
1.018% of the compile), spread across 1,150,601 values. Ownership is 29.064 M
Ir (6.15% of validation, 0.176% of the compile) for 1,330,350 instructions.
Nothing here demonstrates the same invariant being proved twice on the same
state, and no precisely scoped follow-up clears the repository's evidence bar.
All validation semantics and production modes are therefore unchanged.

## Current state and frozen experiment

Before instrumentation, `main` was
`bf84b0202a4d3268587d015e6c19e5ae1f785e4a`, tree
`11ad06d91ad7f99dc0b2cc6e091814422b6d17c5`. Issue #564 was open,
unassigned, and had no comments; no active pull request claimed it. Recent IR
history is owned by David Gonzalez Martin, but the repository has no CODEOWNERS
route for an independent measurement reviewer. The relevant live guidance was
`AGENTS.md`, `docs/agents/frontend.md`,
`docs/agents/frontend/foundations.md`, `docs/agents/benchmarking.md`,
`docs/ir-validation-boundaries.md`, and `tools/throughput/README.md`. The
newest relevant audits were `2026-09-12T224432Z` (the old x86-64-v3
Callgrind observation) and `2026-09-11T200453Z` (the available Zen 5 scope).

The authoritative cost/work experiment freezes measurement commit
`b564aae8ededa5f7a7866987b5dd30ca0f9cff05`, tree
`719b810e86e2f8bdb73de425b0aa1237b88e6ce9`. Diagnostic-parity commit
`46f96daf2725f9dccc06c16b2d3c60e484d045a4`, tree
`bcb58a46b5f92360de085735cc3cdc35e41a465e`, has identical compiler source
and compiled workload bytes; it adds only the invalid-input parity command to
the temporary workflow. The source archive
hash, producer hashes, generated dependency hash, inputs, outputs, caches,
complete compile commands, profiler logs, and raw profiles are retained under
`docs/performance-audits/evidence/2026-09-13T130555Z`.

The hosted runner was Ubuntu 26.04.1, Linux 7.0.0-1012-azure under Microsoft
KVM, four logical CPUs (two cores with SMT), 16,759,500,800 bytes RAM and 3 GiB
swap. CPUID names an AMD EPYC 7763, family 25 model 1 stepping 1. It exposes
AVX2 but no AVX-512. Tools were Clang 21.1.8, CMake 4.4.3, Ninja 1.13.2 and
Valgrind/Callgrind 3.26.0.

Both producers are non-unity Clang Release builds with tests, sanitizers, LTO
and compiler instrumentation off; debug information and frame pointers are on.
The effective compiler flags are `-O3 -DNDEBUG ... -O2 -g
-fno-omit-frame-pointer -march=native -fwrapv -fno-strict-aliasing
-funsigned-char`. The ordinary producer has `BUSTER_BENCH_ALLOCATIONS=0`; the
census producer differs only by that existing diagnostic option being 1.
Their SHA-256 hashes are
`fd44850984e1a85f82c5750a661ff839b50ccbc1335fff4992dd8246372387d9`
and `e86282d2b62ff6cb7539babb5a05c164205c72bde7f5bcefb253d69c400ffec2`.

The unity argv is:

```sh
ide cc -Isrc -Ibuild-prod/generated -DBUSTER_UNITY_BUILD=1 \
  -DBUSTER_INCLUDE_TESTS=0 -march=znver3 -g -v \
  -fsource-metrics=<metrics> src/buster/apps/ide/ide.c -lm -o <output>
```

The small argv is `ide cc -march=znver3 -g0 -O0 -c
-fsource-metrics=<metrics> small.c -o <output>`, where `small.c` is the exact
40 bytes `int add(int a, int b) { return a + b; }\n`. The unity input file hash
is `d096ab6386ad9030597f5e4fc85f57f96298ecc3ceb2c482f820e9a387cc4ac4`,
the small hash is
`23cf718c49184de97b2e966b9426178d82ba04d4f3b591c7ed5fcb6b623f7afb`,
and generated `paths.h` is
`6f5ec2e76d78108edcd5ccfe4796b148ec2ee6c4a206af22832bacf576a52711`.
The unity workload is 371 lexes over 337 unique files, 30,986,849 translated
bytes, 388,335 translated lines, 348,510 sLOC and 3,397,209 preprocessed
tokens. Callgrind ran the ordinary producer with `--tool=callgrind
--cache-sim=no --branch-sim=no`; explicit `-march=znver3` prevents Valgrind's
masked CPUID from silently selecting `haswell` for the compiled target.

## Exact work census

The successful unity compile reaches one validator, specifically the certified
FAST-input call. Counts are cumulative work reached before an error, and none
overflowed:

| check family | exact work |
|---|---:|
| ownership preliminary function scan | 5,655 functions |
| ownership lowered functions / blocks / instructions | 4,803 / 150,049 / 1,330,350 |
| owner-map explicit clear | 5,321,400 bytes |
| globals / relocations / prior-relocation pairs | 14,409 / 6,349 / 819,765 |
| aliases / module initializers | 0 / 0 |
| value blocks / block parameters / values | 150,049 / 29,934 / 1,150,601 |
| value provenance calls / referenced label blocks | 1,150,601 / 0 |
| semantic blocks / parameters / incoming values | 150,049 / 29,934 / 67,505 |
| parameter provenance calls | 29,934 |
| semantic instruction-chain visits | 1,330,350 |
| operand IDs / target IDs / result definition+type relations | 1,286,755 / 207,985 / 1,120,667 |
| opcode-operation / terminator checks | 1,330,350 / 1,330,350 |
| conversions | 151,911 |
| calls / fixed arguments | 36,853 / 97,114 |
| provenance-bearing instruction forms | 221,066 |

`ir_function_instruction_owners` and `ir_validate_block_instructions` each
visit the 1,330,350 successful instruction population once. The former proves
a simple, in-range, uniquely owned chain; the latter proves IDs, result/type,
opcode and terminator obligations. Equal populations do not make them the same
proof. Similarly, target validation and CFG publication both see 207,985 edges,
while parameter validation and publication see 29,934 parameters and 67,505
incoming values; they operate at different boundaries and state.

There is no source-range validation in this canonical verifier. The
"provenance" numbers above are label-value, label-storage and label-block
provenance. Source ranges are carried elsewhere but are not visited by
`ir_validate_canonical_module`; reporting a source/provenance family as one
combined population would be false.

The small compile independently exercises one function, one block, four
ownership and four semantic instruction visits, three values, three operand
IDs, three result relations and four opcode/terminator checks. Its validator
cost is 1,638 Ir of 122,981,881 total (0.00133%).

## Cost attribution

Optimized inlining makes a function-symbol-only breakdown incomplete. The raw
Callgrind stream was therefore decoded by source line. "Exclusive" is Ir
charged directly to the live source range, "direct child" is callee Ir charged
at call sites in that range, and "source subtotal" is their sum. These rows
are disjoint. They plus the explicitly unattributed row equal the validator's
472,211,973 Ir; no nested inclusive totals are added twice.

| live source family | exclusive Ir | direct-child Ir | source subtotal | validator | compile |
|---|---:|---:|---:|---:|---:|
| ownership | 270,992 | 28,793,021 | 29,064,013 | 6.15% | 0.176% |
| conversion-legality helper | 3,202,989 | 0 | 3,202,989 | 0.68% | 0.019% |
| globals and relocations | 11,649,427 | 0 | 11,649,427 | 2.47% | 0.071% |
| values, types and value provenance | 41,762,212 | 126,370,599 | 168,132,811 | 35.61% | 1.018% |
| block parameters and edge provenance | 3,036,080 | 11,593,846 | 14,629,926 | 3.10% | 0.089% |
| opcode-operation relationships | 47,360,675 | 9,945,072 | 57,305,747 | 12.14% | 0.347% |
| instruction chain, IDs, results and terminators | 78,968,465 | 0 | 78,968,465 | 16.72% | 0.478% |
| block structure | 1,965,046 | 0 | 1,965,046 | 0.42% | 0.012% |
| module traversal, aliases and initializers | 292,410 | 0 | 292,410 | 0.06% | 0.002% |
| unattributed after inlining/line mapping | — | — | 107,001,139 | 22.66% | 0.648% |

The unattributed 107.001 M is retained, not redistributed: 73.700 M is line
zero, 25.227 M is inlined `ir.c` helper code outside the bounded verifier
ranges, and 8.073 M is other headers/source files. This is why the table does
not pretend to price every check from optimized debug lines.

Useful overlapping drill-downs, which **must not be added** to the disjoint
table, are: value provenance 142.949 M Ir (30.27% of validation), parameter
provenance 11.803 M (2.50%), instruction provenance including casts 12.984 M
(2.75%), conversion legality including cast metadata calls 11.351 M (2.40%),
call/signature relationships 2.310 M (0.49%), and the global relocation-pair
loop 10.957 M (2.32%). Inside the instruction-chain family, header/type/storage
is 33.029 M, operand IDs 9.821 M, target IDs 2.850 M, result definition/type
17.162 M, and terminator/chain advance 16.106 M. The JSON output records exact
ranges and inclusive/exclusive fields.

## Surrounding canonical preparation

There are two preparation callers in this compile. The driver call performs
4,803 promotion offers, one validation on the 1,330,350-instruction pre-FAST
state, 4,803 FAST functions, and 4,803 CFG publications. Codegen calls the
same preparation entry after those completion flags are set; it performs no
validation or transforms and accounts for the other 4,803 publication visits.
Existing CFG counters report only 4,803 actual CFG builds, 207,985 unique edges,
29,934 parameters and 67,505 incoming values. Callgrind prices the first call
at 1,246,700,050 Ir and the second at 316,815 Ir. The aggregate preparation
function is 1,247,016,865 Ir; its largest direct children are validation
472.212 M, CFG publication 241.149 M, and FAST compaction 205.078 M.

Those surrounding visits are reported because they touch the same populations;
they are not asserted redundant. In particular, the 316,815-Ir second entry is
already only 0.0019% of the compile, and publication establishes the backend
entry representation after canonical preparation.

## Instrument and validation

The only product-source change is narrow counters added to the existing
`BUSTER_BENCH_ALLOCATIONS`/`-fsource-metrics` path. They use the existing
calling-thread TLS accumulator and saturation behavior. In ordinary builds
the recording macro does not evaluate arguments and neither hook nor storage is
compiled. There is no timer, shared mutable state, per-function allocation or
new telemetry framework.

A registered unit test constructs a hand-counted one-block chain with exactly
three instructions (`ARGUMENT`, `CALL`, `RETURN`) and asserts: one validation,
one function/block/value, three ownership and semantic visits, 12 owner-clear
bytes, one operand ID, one result relation, three opcode and terminator checks,
and one call check. The instrumented Release test job passed all
2,652,851 assertions and 50 module tests. This is the nonsampling ground truth;
the separately compiled four-instruction small input is coverage, not its
substitute.

Ordinary and census producers emitted the same 38,789,936-byte unity artifact,
SHA-256 `ee4cba69f79168804c829d83284bf7e3202789d36d561288f5442fc9a94d1d5f`,
and the same 2,400-byte small object, SHA-256
`804e9f4f1f979742b0f120c1be2e705d88233120e493742b51890b826d3dbbda`.
The ordinary metrics contain no `ir_construction.*` fields. The ordinary,
census and Callgrind outputs compare byte for byte. On diagnostic-parity commit
`46f96d`, a second authorized runner compiled the one-line invalid input
`int f(void) { return missing; }`: both producers returned status 1 and emitted
byte-identical stdout
(`92206a2d667fae58f46cbfa301ddd134729277fb2642f74bda9792d37b9bcd4f`)
and empty stderr. That runner identified an AMD EPYC 9V45, family 26 model 2,
with AVX-512; its separately built producer hashes and complete failure log are
retained under `diagnostic-parity` rather than mixed with the authoritative
EPYC 7763 cost run.

On the measured revision, the dedicated census/artifact workflow, compiler
throughput workflow, Release self-host fixed point, GPU acceptance and the
instrumented Release tests passed. The diagnostic-parity step on `46f96d`
also passed. Its following profile repeat failed immediately because the
AVX-512 producer contained an EVEX instruction Valgrind 3.26 did not decode;
that run is retained as a failure and supplies no cost numbers. The temporary measurement workflow causes
the general workflow-lint precheck to fail because it is not a permanent
approved workflow; it is removed from the submitted report revision. Final
ordinary/sanitizer/frontend/mode status is reported on PR #576 rather than
invented here while those jobs are still running.

No laptop build, test, self-host, profile or benchmark was performed. No
authorized Zen 5 host, PMU event, whole-job dedicated timing slot or real
independent reviewer was available. Consequently there is no wall-time, peak
RSS, hardware-retired-instruction, native AVX-512 or Zen 5 throughput claim.
The preliminary run that let Valgrind select `haswell` is retained under
`preliminary-haswell`; it measured 476,568,017 validator Ir of
16,510,062,894 but is not used for the disposition. Neither series may be
extrapolated to native AVX-512.

## Reproduction

The exact hosted commands and tool output are in `build.log`, `census.log` and
`callgrind.log`. Rebuild the two producers from the frozen commit with the
retained CMake caches/compile commands, run the two argv above, then:

```sh
valgrind --tool=callgrind --cache-sim=no --branch-sim=no \
  --callgrind-out-file=unity.callgrind ./build-prod/Release/ide cc \
  -Isrc -Ibuild-prod/generated -DBUSTER_UNITY_BUILD=1 \
  -DBUSTER_INCLUDE_TESTS=0 -march=znver3 -g -v \
  -fsource-metrics=unity-callgrind.metrics src/buster/apps/ide/ide.c \
  -lm -o unity-callgrind
gzip -n unity.callgrind
python3 summarize_callgrind.py unity.callgrind.gz \
  --validator-total 472211973 > attribution.json
```

Raw profiles (the two unity streams losslessly packaged as deterministic
`.gz`), line/function annotations, exact metrics, compressed and decompressed
manifests, and the parser are immutable evidence beside this report. GitHub Actions run
34758731305 and artifact 10317209392 retain the corrected hosted job as an
additional short-lived copy. Run 34759148980/artifact 10318761068 retains the
successful diagnostic parity and failed EVEX profile repeat. Run
34758100399/artifact 10317373646 is the retained preliminary target mismatch.
