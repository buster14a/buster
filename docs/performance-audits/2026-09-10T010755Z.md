`2026-09-10T010755Z` (source audit; hardware measurements pending; **isel:
reuse AArch64 direct-call facts in the operand walk (#132)**).


## Scope and classification

Base: `ef99ec72cf0f17c8818a1d4b4c106fd5908b5eae`. This is a source-verified redundant traversal and an **unmeasured performance hypothesis**, not a demonstrated speedup or a reproduced miscompilation. It continues #132/#128 without claiming either complete.

`machine_select_canonical_function_aarch64` already owns the byte-per-value `direct_call_uses` projection. Its separate instruction/operand sweep is folded into the existing target-order operand walk, before the local-promotion early-continue. No new stream, allocation, full-function pass, permanent row field or SIMD primitive is introduced. One full instruction-array sweep and its operand iterations disappear; that is a source-level count, not measured retired instructions.

The states remain unused (0), exclusively matching direct-call callee uses (1), and other instruction-operand uses (2). Other uses are absorbing, so storage versus linked traversal order cannot change the answer. The defining FUNCTION symbol is still compared against each eligible CALL symbol. Repeated value IDs are not independent vector-scatter destinations. The stateful target selector remains scalar. Incoming-edge policy is unchanged. Only state 1 materializes zero; states 0/2 retain the symbol address.

## Regression and existing ownership

The existing production-selector module gains shared/repeated direct callees, escapes before and after direct calls, indirect/nullary calls and a no-call control. An independent per-value scalar use census checks zero versus symbol materialization. Reversing instruction storage preserves linked target order and remaps definitions, canonical source rows and extra-row IDs. Ordered selected rows, immediate/call-target contents and stack-slot shapes must agree across storage layouts and checked/prevalidated entry points. These tests still need candidate CI at audit creation.

#42 was completed by #269: no unused declarative matcher or full fact builder is restored. Four-byte type facts, candidate rows/ordinals, signature plans and compact opcode metadata already exist. #44 retains ownership of canonical address/provenance facts. Address recovery, legality, metadata decoding, template lookup and capacity estimation are unchanged. The migration map maps historical Forgejo #595 to GitHub #42 and #597 to #44; current GitHub numbers are not remapped. Open #362's Win64 selector/test changes do not overlap this AArch64 walk or `machine_select_test.c`.

Read before implementation: current AGENTS and machine/SIMD/parallelism/build/testing/benchmarking guides, #128 and its negative-experiment handoff, main's newest `2026-09-09T163649Z` audit, and selector audit `2026-09-07T222813Z`. The narrow parameter-copy experiment did not establish an end-to-end gain and is not retried. Open issue pages 1-3 and PR pages 1-2 reached empty terminal pages, but large issue responses were display-truncated; focused searches/reviews do not establish exhaustive history coverage. Exact `direct_call_uses` issue/comment search returned no result.

## Evidence and acceptance

No local compiler build or test was run. Baseline [CI run 34423464502](https://github.com/buster14a/buster/actions/runs/34423464502) passed ordinary self-hosting, repeated-generation evidence and Release regressions on the exact base. Candidate validation remains pending at creation of this audit. Keep the PR draft pending final-SHA full platform/combination, sanitizer, four-mode and self-host gates. The locally read source archive was older (`bf9afdbe...`); the submitted tree is instead prepared on the exact base with original blob checks.

Use the existing native `bench_throughput` harness, frozen identical inputs/flags, all four modes and `--require-identical-output`. Its ordinary native-x86 hosted guard is a control and does not exercise this changed AArch64 path. Add a separate series with `--flag -target --flag aarch64-unknown-linux`. `bench-select` uses `target_native`; only an actual AArch64 host can price this selector with that existing diagnostic. Retain exact commands, binary/input hashes, all raw samples, paired uncertainty, CPU/OS/compiler/affinity/SMT provenance and memory. Building Buster and running its output are separate metrics.

Structural acceptance: zero additional scratch/row storage and exact selected/output identity. Before accepting a speedup, require representative whole-compiler paired improvement and no material code-size/RSS regression, covering short inputs, calls, control flow and large workloads. The hosted broad regression guard is not evidence for a small speedup.

**No timing gain, PMU result, physical Zen 5 evidence, Zen 4 nonregression, emitted-ISA inspection or vector-kernel result is claimed.** AVX2/AVX-512 classification, stable compaction, template/shuffle/scalar-scatter costs and broader address projection remain unimplemented/unmeasured. No losing vector prototype was produced. This bounded scalar work-elimination candidate must be priced before adding another projection.
