`2026-09-11T191710Z` (Linux x86-64, shared container, Clang 18.1.3).

## Verified opportunity

Base: `44a90fcbff44988624878aff9db3346a8bc6550b`. Merged #278 already
separates whole frame objects, and #417 already classifies AArch64 incoming
reads. Neither change is duplicated here. The remaining verified opportunity
is distinct byte ranges in one frame object, previously chained together.

`machine_schedule_memory_access` publishes explicit UNKNOWN or STACK_RANGE
facts after the existing nonvolatile certificate, slot identity, and complete
byte range have been checked. Each frame object owns 1/2/4/8 eight-byte
dependency cells. Larger objects fold indices modulo eight; collisions add
conservative ordering. An operation touches every covered cell, including
unaligned overlaps. Unknown operations flush all pending cells, and calls,
atomics, fences and physical-register rows keep their barriers.

Work is bounded by eight keys per memory operation, with at most 23N+8 graph
edges. Scratch overflow rejects the schedule before allocation. Classification
and range scratch remain entirely inside the QUALITY pressure gate; the FAST
selection/allocation path is unchanged. The explicit mutable-vreg compatibility
exception remains necessary and is not removed.

## Validation and limits

Trusted build: TCC-bootstrap build driver, Clang 18.1.3 Debug, non-unity,
`./build.sh generate --linker DEFAULT -DBUSTER_UNITY_BUILD=OFF` then
`CMAKE_BUILD_PARALLEL_LEVEL=1 ./build.sh build -t ide`.

`BUSTER_TEST_JOBS=1 build/Debug/ide test --verbose=1` passed all 1,486,784
assertions in 46 modules. The machine module passed 476,320 assertions.
New controls prove that disjoint fields in one object actually reorder and
reduce spill+reload traffic on x86-64 and AArch64; overlapping accesses retain
source order, modulo collisions remain conservative, replay loses the producer
certificate, and malformed/overflowing ranges default to UNKNOWN.

The unchanged base self-host gate was run by the coordinating task and passed
with a byte-identical 38,911,304-byte FAST stage pair plus MIR_STACK/NONE
checks. Candidate Release self-host, the full mode matrix and representative
paired compiler-throughput acceptance remain to be run. This shared host is
contended; no timing improvement or FAST non-regression is claimed from these
structural tests. Per-object certification for mixed volatile/nonvolatile
functions remains follow-up scope; #41 is not closed by this first change.
