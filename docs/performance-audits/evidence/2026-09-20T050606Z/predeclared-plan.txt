Final same-build-path confirmation, specified after the same-source cross-build control exposed an approximately3.1-3.3% aggregate-ABI difference and an80-byte .text relocation. All earlier worktree-path comparisons are exploratory and cannot alone establish source-change benefit.
Build each revision serially in /home/david/bench/issue791-ready-853 with identical configured Clang Release flags and TCC bootstrap policy. Validate self-host/test_all on every measured source, then copy trusted binaries to immutable named directories. Verify exact src+tests patch identity and compiler hashes. Baseline build twice with unchanged source must be byte-identical. Main4b2041c4; pre854 parent of23f88492; post85423f88492 (source-identical to cache-removal238ca910); minimal85694231973; minimal857fc5de3f3. The compact8538374c5e1 binary was already built in this exact directory with identical flags and passed self-host/full tests; retain its recorded source/binary identity.
Two warmups and two fixed rounds40pairs,20AB/BA blocks, CPU2 host.lock, default lowering fast object output, byte-identical outputs for all native pairs; no trimming/extensions or stopping for favorable results. Point medianblockratio, bounds third/eighteenth sorted ratios; report every comparison including failures/inconclusive results.
Main A/A default six. Four default-six+macros/aggregate cases: pre854->post854, main->post854(cache removal), main->minimal856, main->minimal857. Also main->compact853 same corpus. Additional #854 local-backed large_function (-fno-frontend-ssa -fno-canonical-local-promotion), and main A/A plus main->minimal857 many_functions scale8. Total98 native rows, nominal simultaneous coverage>=96.05% under independent-block assumption. Require both rounds to establish a scoped benefit, inspect regressions and A/A controls. These fresh matched-build comparisons determine readiness; no further variant or sample-count changes are planned. No frozen-unity throughput claim; exact-source fixed points are required correctness gates.
