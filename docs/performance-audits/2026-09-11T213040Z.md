`2026-09-11T213040Z` (Linux x86-64 container, Clang 18.1.3; **Address facts
rebased with TLS and scheduler metadata integration**).

PR #446 was reconciled with main
`dae74aa6099f93069af82274f89df7b8471eab2d`, which includes the merged vector
contract, metadata cleanup, scheduler certificates, and Windows/Darwin TLS
lowering. The only manual rebase conflict joined the shared selector's
introductory comments with the address-cache helpers; both were retained.
`git range-diff` confirmed no functional change to the #44 patch, including
the correction that reuses materialized addresses for zero-offset copies.
Global/TLS materialization and the scheduler certificate producer remain
those from main.

The first rebased remote commit,
`172067706d8509a365659df5629b44defe73f6dd`, failed the hosted Release build:
four TLS descriptors in main still initialized the removed
`MachineOpcodeInfo.name` field. This record applies the identical four-line
repair from `215cac3b3e76a2c5697ebad739d038a5bba55a86`, locally committed as
`82dbfaa18b4ee754357a07141cac4a91f108495e`. Only those unused initializers
are removed; TLS operand, clobber, memory, and encoding contracts are unchanged.

The repaired revision is validated through the ordinary TCC-bootstrapped driver:
`CMAKE_BUILD_PARALLEL_LEVEL=1 BUSTER_TEST_JOBS=1 ./build.sh build --config Release -t test_all`.
This uses Clang 18.1.3, non-unity Release, tests enabled, and the existing local
configuration with debug information and frame pointers enabled. The build
and full suite exited 0: **1,491,013/1,491,013 assertions, 47/47 modules**.
It is a correctness run, not a paired throughput experiment. The
[summary](evidence/2026-09-11T213040Z/release-suite-summary.log), compressed
complete log, exact compile commands, and binary hash are retained in
[the evidence directory](evidence/2026-09-11T213040Z/).

Earlier evidence remains historical:
[the initial implementation and census](2026-09-11T193928Z.md) and
[the coordinated diagnostic throughput run](2026-09-11T200803Z.md) use their
recorded older base and binaries. Their self-host, matrix, sanitizer, and timing
results are not relabeled as validation of this repaired rebase. The older
Windows CI attempt at `94095cb7` failed a process-wait assertion after Node
printed all 1,504 Wasm checks passed; its same-head retry was cancelled when
the branch was rebased. The cause of that process-wait failure remains unknown.
Both original CI failure logs are retained, including the concrete TLS metadata
compile error on the first rebased head.

No new performance result is claimed. Final repaired-head hosted CI and
dedicated-platform performance acceptance remain pending; the PR stays a draft.
