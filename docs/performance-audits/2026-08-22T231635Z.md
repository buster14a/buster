`2026-08-22T231635Z` (Linux x86_64, Zen 4 7940HS; **the by-value preprocess
result loses its cold half, and an exact copy census re-ranks the compile's
memcpy traffic**).

Round two of the data-oriented audit, on `8c0a8cfd`: re-run the
`2026-08-16c` struct-size survey against a tree where `CToken` is already
12 bytes and the L1d budget has been paid down twice, then census
allocation and copy traffic in the non-pessimization sense and take what
the census names. The `2026-08-09b` IrInstruction SoA row split stays
out of scope — it is measured negative and recorded do-not-retry.

**Budgets before** (clang Release `ide`, unity stage-1 compile):
9.828.033.404 instructions, 1.811.385.465 branches, 21,6 M branch misses,
3.534.292.309 L1d loads, 92,3 M L1d load misses (2,62% of loads), 5,90 M
fills from DRAM, IPC 2,61.

**The instrument is the finding.** Neither result is visible to a DRAM
ranking. An `LD_PRELOAD` interposer replacing `memcpy`/`memmove`/`memset`
tallies exact bytes and calls per return address and dumps
`/proc/self/maps` beside its table, so every site rebases to a static
vaddr for `llvm-symbolizer`. Stage 1 moved **1.013.955.550 bytes through
4.703.078 memcpy calls**, and zero memmove calls. That census is
deterministic and load-immune, which no sampled survey here is; it ranks
by *removable* traffic, which is what `2026-08-22T091548Z` concluded a
miss share does not.

**Two thirds of the compile's copy bytes were one struct.** `pahole` over
the `-O0` unity TU put `CPreprocessResult` at 712 bytes, and the census
attributed **658 MB across 1,47 M calls** to copies of exactly that size:
546 K in `c_ir_constant_initializer_bytes_legacy_core` alone, the rest in
the type-parse machine's frame pushes. It is passed *by value* through
about a hundred and fifty parameters while its hot readers touch four
members — `tokens`, `spelling_base`, `target`, `dialect`. The target data
layout (read three times per compile) and the four measurement tables
(read by `-v` reporting and `-fsource-metrics=`) were 548 of those bytes.

They now sit behind one pointer, `CPreprocessDetail`. Hand-built results —
the token-space literals the integer-constant evaluator builds — carry no
block and read as an all-zero one through the header-inline
`c_preprocess_detail`, which is what their inline members held before.

    CPreprocessResult    712 -> 176 bytes
    CTypeParseFrame     1336 -> 800
    CIntegerIrBuilder   1616 -> 1080

Eleven order-alternated CPU-pinned pairs of clang-built binaries compiling
the unity `ide.c`, outputs byte-identical:

    instructions           -61.594.546  (-0,627%)  11/11
    branches               -13.748.885  (-0,759%)  11/11
    L1d loads             -110.417.500  (-3,12%)   11/11
    L1d load misses           -686.331               9/11
    branch misses             +165.329               2/11
    fills from DRAM/IO         +54.831               5/11

Stage-1 memcpy traffic fell to **309.848.866 bytes over 3.713.227 calls**:
-69,4% of the bytes, and 990 K copies that no longer reach libc at all.
The branch-miss row is a real but small regression and the DRAM row does
not resolve; both are recorded as measured rather than explained.

**Re-ranked, the census stops being about bytes and becomes about calls.**
What was left after the split was 2,6 M libc memcpy calls whose *mean is
four to seven bytes* — x86 instruction encodings, in
`machine_x64_emit_variable_memory_encoding` (1,39 M),
`machine_x64_emit_exact_recipe` (777 K) and the three x86 metadata
emitters (405 K). `memory_copy_small` in `base.h` covers every count up to
sixteen with three overlapping power-of-two moves, reads and writes
exactly `[0, count)` so it is a drop-in, and falls through to `memcpy`
past sixteen.

**Substituting it at all five sites measured +43,8 M instructions, 0/11.**
Bisecting by file, on one fixed tree:

    no helper                       9.766.616.726
    two machine_x86_64 sites        9.815.347.667   +48,7 M
    all five sites                  9.810.465.330   +43,8 M
    three metadata sites only       9.761.712.380    -4,9 M

The helper's own code generation is not the problem — disassembled, it is
eight to ten instructions with no spill. The cost is second-order:
expanding it inside those two hot encoders perturbed *their* register
allocation, and the reloads (+20,8 M L1d loads over the five-site variant,
about 9,6 per call) cost far more than the 2,2 M calls removed. Only the
metadata third shipped:

    instructions            -4.928.801  (-0,050%)  11/11
    branches                -2.360.509             11/11
    L1d loads               -2.570.488             10/11
    fills from DRAM/IO        -115.998             10/11

**Cumulative, eleven pairs, byte-identical:**

    instructions           -66.551.770  (-0,677%)  11/11
    branches               -16.109.561  (-0,889%)  11/11
    L1d loads             -115.497.387  (-3,27%)   11/11
    L1d load misses           -809.862               10/11
    fills from DRAM/IO        -235.007                8/11
    branch misses             +125.942                4/11

Copy traffic over the same interval: **1.013.955.550 -> 308.035.448 bytes
(-69,6%)** through **4.703.078 -> 3.308.578 calls (-29,7%)**. Branch misses
are the one counter that ends worse, by 0,58% of the budget, and no
mechanism was established for it.

Two predictions failed on the way and are worth the space. The `+44 M`
first seen on the `SELF_HOST stage 1` counter was dismissed as the changed
input tree — it was real, and the pair harness reproduced it at `+43,8 M`;
a source-growth alibi needs the arithmetic done (47 lines is about 0,7 M
instructions here at 355 instructions/byte), not assumed. And the
predicted mechanism, mispredicted branches on the size ladder, was wrong
in the direction it was wrong in: branch misses *improved* by 135 K. The
counter that named the cause was L1d loads.

**Gates, every commit:** `ide test` green (309.490 unit assertions, 39/39
modules), `test_self_host` byte-identical at the fixed point,
`test_all_combinations_ci` green before each push (Linux-only; Windows and
aarch64 stay remote-pending).

**Left untaken, in the order the census leaves them.**
`machine_stream_flatten @ machine.c:1288` is now the top site by bytes —
80,9 MB over 19,6 K calls across five callers, chunked 16 KB builder
streams copied into one contiguous array at `machine_function_builder_finish`;
most functions fit a single chunk, so this is a "write it in its final
place" arena-shape change of the kind `c_preprocess` already made for its
token stream (PR 500). `link_objects @ link.c:717` (34,8 MB over 10 calls)
and `object_from_canonical_codegen_module @ object.c:8575` are the same
shape one level out. `c_translate_source @ c_source.c:541` is 564 K calls
at a 48-byte mean, the largest remaining *call-count* site and the one
place a copy helper has not been tried. The two machine_x86_64 encoder
sites are do-not-retry as a straight substitution; if they are revisited
it must be with the register pressure of the surrounding function as the
thing being measured, not the copy.

**Reference points for the next audit:** stage-1 instructions
9.761.590.287 at the byte-identical self-host fixed point; memcpy traffic
308.035.448 bytes over 3.308.578 calls; `CPreprocessResult` 176 bytes.
Reproduction: the interposer is `memcpy_census.c` in the session
scratchpad — an open-addressed table keyed on `__builtin_return_address(0)`
with naive copy implementations, dumped at exit with `/proc/self/maps`;
the A/B harness runs eleven order-alternated pairs under `taskset -c 5`
with both binaries compiling the same unity `ide.c` to the same `-o` path
and compares the outputs byte for byte.
