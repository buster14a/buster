`2026-09-10T191815Z` (Linux x86-64, virtualized AMD EPYC 9V74, Clang 18.1.3;
correctness integration only; **Integrate lazy syntax diagnostics with integer
validation and retained iOS evidence**).

## Revision and scope

This is correctness integration for #379 / #248, not performance admission.
Submitted source `04ab9706a99757e5d9f237d9ba17bd9190f19da0` has tree
`2c168aa90922ca5df83260ee99767f25d83f53f6`, identical to local tested merge
`1b1a0bc8477d40716875f32413b9b012a1d037de`. It preserves original #379
head `01537e35` and integrates the accepted #366/#395 source through staging
commit `9e4a3a8616acf16bfae8edc6df74fc0ba4290f71`, tree `5bd21001`.
The latter includes main `348e417d` and the #396 iOS lifecycle diagnostic repair.
Staging ancestry does not mean these prerequisite PRs had already merged at
publication; their separate acceptance gates remain in effect.

The test-file merge preserves both complete additions: lazy diagnostic storage
coverage and #372's integer-spelling/language checks. Integration also requires
passing the parse arena through `c_parser_validate_integer_token` to
`c_parser_diagnostic`, including both declaration and function-body callers.
Clean parses retain null diagnostic storage, while the first error receives
the original arena-owned contiguous capacity and diagnostic lifetime.

No diagnostic expectation, test registration, warning, CI shard, mobile
lifecycle deadline or performance threshold is weakened. The original census
and `2026-09-10T011438Z` audit remain unchanged.

## Local acceptance

On the exact source tree above, Linux x86-64, trusted Clang 18.1.3:

- Release unity: **922884/922884 assertions, 46/46 modules**, exit 0.
- Debug split, strict ASan/UBSan/bounds: **914449/914449 assertions,
  46/46 modules**, exit 0. Complete PTY logs retain their result footers.
- All ordinary, machine and canonical self-host stages finish. Ordinary stage
  1 and stage 2 are byte identical at **38689064 bytes**, SHA-256
  `8e847069fcbb31ac8a29416011b7ee09466625f5194a850362385eb308abbcd0`.
- All **24 mode-matrix legs pass**: four native executions and twenty
  independent object/disassembly oracles. QEMU was not on this run's PATH;
  foreign oracle checks are not counted as executions.
- Before this integration, combined prerequisite tree `5bd21001` passed
  921400 Release and 912965 sanitizer assertions, 46 modules each, and all
  self-host stages with a 38688968-byte identical ordinary fixed point.

The integrated trusted compiler SHA-256 is
`469d65c1f269916bdc0a35f89aedd17a94d816553ffe0f15a6c44a99d7852dc2`.
The local runtime denies LeakSanitizer access to /proc. Only local leak
detection is disabled; `ASAN_OPTIONS=detect_leaks=0:halt_on_error=1` and
`UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1` are recorded explicitly.
Repository CI settings are unchanged.

Commands, from the repository root with the existing verified toolchain on
PATH, use `BUSTER_TEST_JOBS=2` and `CMAKE_BUILD_PARALLEL_LEVEL=2`:

```sh
./build.sh generate --cc clang --config Release --linker DEFAULT
./build.sh build --config Release -t test_all
./build/build test_self_host --config Release
./build/build test_mode_matrix --config Release
./build/build generate --build-dir build-sanitize --cc clang --sanitize --config Debug --linker DEFAULT
./build/build build --build-dir build-sanitize --config Debug -t test_all
```

The two build directories are independent. Full test commands were captured
with `script -q -e -c COMMAND LOG`; raw session logs are
`379-integrated-clang.log`, `379-integrated-sanitize.log`,
`379-integrated-selfhost.log` and `379-integrated-modes.log` in the coordinating
session's scratch directory. Compiler timings printed during self-hosting are
correctness diagnostics, not matched performance measurements.

## Remaining gates

Old-head hosted CI retains the initial iOS failure and its single bounded
retry: both app suites passed, but simulator shutdown failed. New-head CI
must run with #396's bounded raw-log retention and native/helper exit status.
This is a changed-source integration, not another blind unchanged-head retry.
No successful native iOS run is claimed in this audit.

The original representative full-corpus timing, memory and compiler-text
budgets and physical Zen 5 / separate Zen 4 acceptance under #46 remain
outstanding. This virtualized EPYC 9V74 session establishes no physical-core
exclusivity, idle SMT sibling, usable PMU or performance gain. #379 stays draft
until its complete declared acceptance is satisfied; #248 and #128 stay open.

The strict audit checker reports the same single malformed historical opener
(`2026-08-24T131821Z`) on prerequisite and candidate trees. This new entry is
correctly indexed; the old audit and checker are preserved without a waiver.
