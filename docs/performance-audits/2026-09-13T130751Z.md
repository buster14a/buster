`2026-09-13T130751Z` (remote source and published-evidence review; **SSA
finalization: ordered work elimination and bounded SIMD, no new execution**).

The canonical detailed report is
[SSA construction and graph maintenance](../astra-simd-05-ssa-graphs.md).
It contains the source trace, evidence provenance, exact algorithms and
invariants, complete cost models, alternatives, crossover conditions,
validation gates, and bounded implementation handoff. Keep substantive updates
there rather than duplicating the report in this entry or in issue comments.

Analyzed main: `5865fa7add27908656aedf28a3c0e6a3de47dc04`, tree
`950eae4171f20ece45f1092fbc311c950665ed5b`. Publication base:
`5217113aa33a74cd78524c29bd8addfc0e219ef0`, tree
`a1a85005b5e6ebc48b5af6cfb66a9b31cc73ec8d`. The intervening comparison changes
only Vulkan installer retirement and its CI workflow; analyzed SSA sources and
guides are unchanged.

## Disposition

First compare fusion of the two adjacent dense-ID/retained-value scans in
`c_ir_ssa_finish`. Then compare reuse of its expired liveness queue as an
ordered alias frontier against an independent 16-way root-identity classifier
using the existing SIMD vocabulary. The first fusion is independent of the
classifier; the alias frontier and classifier overlap and their savings must
not be added. Preserve every representative, ordering decision, source and
metadata mapping, memory-recovery rule, and existing verifier boundary.

The source supports fewer enumerated old value IDs: `3N -> 2N` for fusion,
and `3N -> N+A` with the alias frontier, where A is the non-root population.
This is not a measured instruction, time, RSS, or whole-compiler improvement.
The SIMD proposal performs actual parallel identity comparisons, not a claim
of vectorizing dependent pointer walks. No gather builtin or new IR is needed.

Reuse existing ownership in #447/#497/#548 and #534/#546. Respect active
validation attribution in #564/#576 and feature-tier correctness in #574.
Do not reopen #447, duplicate its issue, or expand #36 retirement acceptance.

## Evidence and gates

The report distinguishes historical 9700X sampling, later hosted construction
counters, and the newest checked-in x86-64-v3 Callgrind/fill audit. Current
per-function alias/fan-in/frontier distributions and all proposed crossover
measurements remain unavailable, not inferred from aggregate counts.

No compiler, prototype, census, regression test, sanitizer, self-host,
mode-matrix, benchmark, or PMU experiment was run for this research, locally or
remotely. This is a documentation-only publication. The audit generator and
full repository documentation checks were not run under the no-local-execution
contract; the inspected timestamp convention is followed. No production or CI
files, closed index, old audit, or another session's branch are changed.
