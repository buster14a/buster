`2026-09-10T190027Z` (Linux x86-64 KVM guest, Clang 18.1.3 and GCC 13.3.0;
**Z02: invalidate rejected throughput summaries and preserve sealed
evidence**).

## Recovered defect and scope

Main `348e417d16c9b6f9969d2260c491428bff6d7feb` reproduces #399,
recovered from the offline Z02 patch and #46's coordination record. The
baseline native harness passes 30 core and 92 integration assertions, but
scenario 0 leaves `summary.json` with `valid:true` after changed metadata makes
replay return 2. Scenarios 7 and 8 return 2 for output nondeterminism and unequal
work; their partial Markdown still says zero confirmed regressions. The CI
publisher consumes an existing Markdown report even after failure.

`tp_compare` now invalidates both derived reports before validation and removes
new reports after closing streams when late validation or I/O fails. Cleanup
attempts both paths and reports an undeletable path. It does not delete any
sealed evidence or completion marker. Exit 0 and exit 1 retain complete reports;
exit 2 has no verdict when cleanup is possible. Consumers must honor failure
status after filesystem errors or interruption.

The recovered patch was applied unchanged before adding a Unix child-only
file-size-limit regression for buffered report write/close failure. A pre-existing
ignored `fread` result prevented strict GCC compilation of the harness; the
fixture now checks opening, reading and closing its child log.

## Validation

The trusted host compiler is Ubuntu Clang 18.1.3; the independent compiler is
GCC 13.3.0. This is a virtualized EPYC 9V74 host, not physical Zen 5/Zen 4
acceptance. All timings inside the tested bundles are synthetic fixtures.

```sh
clang -std=c11 -O2 -Wall -Wextra -Werror -Wpedantic -fwrapv \
  -fno-strict-aliasing -funsigned-char tools/throughput/tests.c -lm -o /tmp/throughput-tests
/tmp/throughput-tests /tmp/throughput-results
```

Use distinct output directories for baseline, candidate and sanitizer runs.
The same command with `gcc` exercises the independent host compiler. The
sanitizer build replaces `-O2` with `-O1 -g -fsanitize=address,undefined,bounds
-fno-sanitize-recover=all`; runtime uses `ASAN_OPTIONS=detect_leaks=0` and
`UBSAN_OPTIONS=halt_on_error=1`. Local leak inspection is unavailable in this
environment; CI sanitizer policy is unchanged.

| Run | Core assertions | Native integration assertions |
| --- | ---: | ---: |
| Original main, Clang Release | 30/30 | 92/92 |
| Recovered patch, Clang Release | 30/30 | 188/188 |
| Final source, Clang Release | 30/30 | 199/199 |
| Final source, GCC Release | 30/30 | 199/199 |
| Final source, strict Clang sanitizers | 30/30 | 199/199 |

Coverage includes both valid verdicts, corrupt seals, missing completion,
malformed/incomplete samples, unequal work, nondeterministic output, preservation
of the completion seal, and nonempty-directory cleanup refusal that still
removes the other report. Unix additionally forces buffered report writes to
fail in an isolated child and verifies no summary survives while the seal stays
valid. The child limit and ignored SIGXFSZ disposition do not affect its parent.

Independent artifact inspection verifies valid JSON verdict contents and absent
JSON/Markdown for every rejected fixture. For scenarios 1 through 9, all six
evidence files plus the completion file are byte-identical to baseline across
all three final runs (189 file comparisons). Scenario 0 intentionally mutates
metadata and removes completion as part of its regression.

This change touches only the standalone harness, its tests and documentation.
Compiler implementation, workload bytes, sample order/counts, schema, thresholds,
allocation/PMU separation and statistical decisions are unchanged. No compiler
speedup, full compiler rebuild, self-host result or physical-hardware admission
is claimed by these local harness checks. Existing submitted-head CI remains
required; final results and merge identity belong in the PR and #390.

#345's process diagnostics and #116's optional assembly-emission experiment
remain distinct unmerged work. #46 stays open for its broader acceptance scope.
