`2026-08-22T195419Z` (Linux x86_64, Zen 4 7940HS; **re-rank both miss surveys
on post-510/512 main; the module-symbol pass of the C lowering still compares
specifier spellings**).  Both surveys re-run from scratch on `8c0a8cfd` —
`tools/branch_miss_survey.py --repeat 3 --cross-check` and
`tools/cache_miss_survey.py --dwarf` — because the standing rankings
(`2026-08-22T144241Z` and everything before it) predate PRs 510 and 512
landing, plus the single-return sweep (PR 511) that moved every line number
those audits quote.  The machine was gated quiet before each capture (load
below 2.5, no concurrent compiler processes); the `--cross-check` columns
agreed with the LBR tally throughout — no PIE skew.

**Budgets on `8c0a8cfd`** (clang Release `ide`, unity stage-1 compile):
9.8725 G instructions, 1.8188 G branches, 21.22 M branch misses (1.167%, down
from 1.246% at `2026-08-22T144241Z`), 3.73 G cycles (IPC 2.58); 92.86 M L1d
load misses (9.4/kinstr), 5.77 M L2 read misses, **5.67 M fills from DRAM**
(down 6% from 6.04 M), L1i 3.87 M, dTLB walks 77 K, page faults 1,845.

**The re-ranked leads, open ones first.**

1. **`c_lower_to_ir`'s module-symbol pass** — the largest open aggregate on
   either survey: 7.19% of DRAM fills at 3.16x its cycles share, 4.35% of L1d
   misses, 2.24% of branch misses, and it feeds the two biggest non-copy libc
   rows (`string_equal`'s `__memcmp_evex_movbe` at 1.32% of DRAM samples,
   `c_token_length` at `c.h:1034` on both line tables).  The cause is visible
   in the source: nineteen `c_declaration_has_token` call sites re-scan each
   declaration's specifier tokens once per spelling — five scans per object
   declaration (`extern`, `static`, `__thread`, `_Thread_local`,
   `thread_local`), four per function symbol (`inline` three ways plus
   `extern`) — each materializing cold spelling bytes through
   `c_token_length`'s oversized guard and running `string_equal`, exactly the
   shape `0f361562` (PR 514) removed from the body lowering.  The well-known
   id mechanism already carries STATIC/EXTERN/THREAD_LOCAL/THREAD_GNU;
   `c_declaration_section_name`'s `section`/`__section__` compares are the
   same fix one enum extension away.  **Taken below.**
2. **The flatten/merge copy cluster** — `machine_stream_flatten @
   machine.c:1288` (3.17% of DRAM samples, 2.88% of L1d samples),
   `link_objects @ link.c:717` (3.21%), `link_native_executable_elf64 @
   link.c:2148` (2.47%), `c_source_map_append @ c_source.c:233` (1.65%),
   `object_from_canonical_codegen_module @ object.c:8575` (1.34%) — together
   ~12% of DRAM samples inside `__memcpy_avx512`.  All are "write it in its
   final place" design changes.  For the flatten specifically: builder chunks
   and the flattened arrays live in the same arena, and
   `MACHINE_BUILDER_CHUNK_BYTES` is 16 KB (682 24-byte instruction rows), so
   most functions are single-chunk and could alias the chunk storage as the
   final array instead of copying — unmeasured, left as the next lead.
3. **`codegen_generate_canonical_module`** — 3.71% of DRAM at 12.63x its
   cycles share; the once-per-module template-cache memset over a
   function-count-scaled arena block (`codegen.c:16119`) plus the attempt
   scaffolding.  The arena is fresh at that point of a compile, so the check
   whether a dirty-watermark can skip the zeroing still stands.
4. **`c_parse.c:11607`** — the per-declaration `__overloadable__` scan, 1.26%
   of branch misses.  Already id-based; the remaining cost is the scan
   itself, which the position index's recorded attribute positions could
   bound the way `652a5bab` (PR 514) bounded cleanup-attribute validation.
5. **`register_allocator_fast.c:2062`** — the spill/reload `slot_needed`
   flag store, 1.17% of branch misses (248 K).  A branchless store must be
   measured in instructions first (the `2026-08-22T114019Z` rule).
6. **`c_source.c:4401`** (`c_preprocess_line_end`, 1.03% of misses) and
   `c_gen.c:23082` (`c_ir_lower_body_initialize`, 1.03%) — single lines with
   no obvious lever beyond what PR 514 already took; unexamined.

**Still at the top and still closed**, so the ranking does not re-open them:
`machine_select_canonical_function_x86_64` is the #1 symbol on every table
(13.97% of branch misses, 23.70% of DRAM fills) but its DRAM lines
(`machine_x86_64.c:4338-4339`, 11% together) are the compulsory ascending row
walk `2026-08-22T114019Z` measured from both directions, and its dispatch
line (`machine_x86_64.c:4048`, 2.03%) is the selector switch
`2026-08-22T141319Z` adjudicated against in both spellings.  The remaining
selector branch-miss lines (`4382`, `4390-4395`, `4503-4508`, ~4.8%
together) are the data-dependent candidate/operand tests inside that same
compulsory walk.  `c_parse_lookup_entity_symbol` (3.86% of misses, 3.23% of
DRAM) and `c_symbol_intern` (3.03%, 3.14%) are the two hash tables
`2026-08-22f` measured at their floor.  `c_parse.c:11315` is
`c_analyze_semantics`' capacity ladder, whose branchless form lost 157 M
instructions in `2026-08-22T114019Z`.  `c_ir_lower_frame_fallback` (3.51% of
misses) is the frame machine; its BODY/BODY_ASSIGNMENT self-loop follow-up
is claimed by the dispatch instalment-2 line of work and is deliberately not
taken here.  `machine_fast_placement_build_prepassed` (7.47%) carries the
`2026-08-22a` lesson that its loop-exit misses relocate rather than vanish.

**Taken: lead 1, the spelling scans of the module-symbol pass.**
`c_declaration_has_token` gains a well-known-set twin that answers every
specifier question of one declaration in a single pass over its tokens — one
shift-and-mask per interned token, the spelling fallback only for tokens the
intern pass never saw — and `c_declaration_section_name` compares the new
SECTION/SECTION_GNU ids instead of two spellings per identifier.  Six enum
entries are added (`thread_local`, `inline`, `__inline`, `__inline__`,
`section`, `__section__`), keeping the set below the 64-bit ceiling with room
to spare.

Measured as eleven order-alternated CPU-pinned pairs of clang-built binaries
compiling the unity `ide.c`, outputs compared byte for byte; the numbers are
recorded below once the harness has run.
