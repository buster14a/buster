`2026-09-11T200803Z` (Linux x86-64 container, Intel Xeon Platinum 8573C, Clang
18.1.3; **Shared address facts: coordinated native FAST throughput
diagnostic**).

Follow-up evidence for #44 / PR #446. The implementation and correctness record
is [2026-09-11T193928Z](2026-09-11T193928Z.md); it remains unchanged. Production
revision `2370f88b7bd214c87b38e37170970f2bb9335579` is unchanged as well. The
tested source plus the first audit was published with verified tree
`670bdc2e59c18baa56ea569dc3e22541087333a5` at remote commit
`96e31ec0174b84dc6434e61338880b063fbcad56`.

The existing native `tools/throughput` harness ran the six-workload `ci` profile
in FAST mode, on CPU 8, with two warmups and two predeclared rounds of twelve
adjacent A/B pairs per workload. This is explicitly `--no-guard` diagnostic
collection, below the harness's twenty-pair guarded minimum. No output-identity
constraint was imposed between different compiler variants. No samples were
trimmed or rerun. The root coordinator paused all other agents' builds, tests,
and benchmarks for this run from 20:06:48Z through 20:07:10Z. Affinity and that
coordination do not establish physical-host exclusivity.

Baseline is `44a90fcbff44988624878aff9db3346a8bc6550b`. The candidate uses the
exact baseline compile flags for the changed `machine.c` translation unit,
relinked with the frozen baseline's other objects, as documented in the first
audit's `comparable-build.json`. All changed production sources are included by
that translation unit; no public layout changed. The native harness itself is
unchanged baseline source, SHA-256
`44c5ff5cc5c5523a17be1b0229e3ed9beaca4cae862ad21c6cbe1fa753b2dcb2`.
Compiler hashes, exact argv, environment, source hashes, observations, and
statistics are retained in [the evidence directory](evidence/2026-09-11T200803Z/).

| FAST workload | Baseline wall median, ms | Candidate wall median, ms | Harness paired change | Baseline / candidate peak MiB |
| --- | ---: | ---: | ---: | ---: |
| tiny_startup | 17.710 | 18.136 | +0.80% | 23.27 / 23.39 |
| large_function | 28.646 | 28.416 | -0.69% | 29.33 / 29.36 |
| many_functions | 25.573 | 24.949 | -2.51% | 28.81 / 28.88 |
| symbol_table | 29.821 | 30.371 | +0.54% | 30.33 / 30.39 |
| control_flow | 62.600 | 62.719 | -0.67% | 38.75 / 38.79 |
| backend_pressure | 153.856 | 158.850 | +0.87% | 55.28 / 55.24 |

The paired change is the harness's combination of round-level paired ratios,
not the ratio of the two unpaired wall medians. Per-round ratios, conservative
confidence intervals, relative MAD, CPU time, RSS, faults, context switches,
output bytes, and throughput units are in `summary.json`. Every wall interval
crosses 1 and is broad: for example backend-pressure intervals are approximately
0.520–3.016 and 0.805–2.481. These observations establish neither a speedup nor
equivalence. PMU and instrumented allocation replays were not requested and are
unavailable/null, not zero. This x86-host diagnostic does not supply AArch64
compiler-throughput evidence.

Collection and strict raw-data replay both exited 0, with `valid=true` and
`guard_enabled=false`. The printed zero confirmed-regression/inconclusive counts
are not performance admission when the guard is disabled. All 288 measured child
observations and 24 warmups completed. The archive `native-fast-ci.tar.gz` retains
all six sealed primary evidence files, the seal, generated inputs, commands,
capabilities, logs, per-invocation metrics, and host observations. Only the last
compiled `.o` outputs are omitted; their sizes and hashes remain in the sealed
samples. Extract the archive and replay with the repository's existing
`throughput compare --output native-fast-ci`. The bundle manifest records the
archive hash and contents. No additional benchmark framework was introduced.

This follow-up changes evidence only. Existing local correctness gates remain
those recorded for the frozen production revision. Final-head hosted CI and
dedicated-host/platform performance acceptance remain pending; PR #446 stays a
draft. The earlier overlapping timing samples remain rejected and are not used
in this result.
