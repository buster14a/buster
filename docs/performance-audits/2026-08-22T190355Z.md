`2026-08-22T190355Z` (Linux x86_64, Zen 4 7940HS; **give the AArch64 selector
a compact row layout and candidate list**).  `2026-08-22e` gave the x86-64
selector one authoritative row projection and left AArch64's matching
four-traversal shape as its explicit next lead.  The current AArch64 selector
still rebuilt every block's linked row list in each of two reverse alias
sweeps, then chased the list again for value classification, compare/branch
fusion, and final selection.  Only `LOAD`, `STORE`, `DEREFERENCE`, and
`BRANCH_IF` can change alias or fusion state.

The target-order value-fact walk now records each block's row count and the
offsets of those four candidate opcodes.  Dense blocks retain no per-row ID
stream: later passes derive a row from `first_instruction + offset` through
the shared `MachineSelectionRowLayout`.  A function with any relinked or
interleaved block gathers program-order row IDs once, preserving the manual IR
and selection-reordering boundary.  The reverse alias sweeps consume the
candidate offsets backwards, branch fusion consumes them forwards, and the
classification and final-selection walks consume the counted row layout.
Aliased results are also recorded in assignment order, so the register-sharing
step visits only the values that actually acquired an alias.

The shared checked selection prepasses now also reject opcodes outside the IR
opcode table before any target candidate-mask shift.  A focused malformed-IR
regression covers both the full and minimal prepasses and both checked target
selectors; explicitly validated entry points retain their existing trusted
contract.

Five alternating same-source, CPU-pinned pairs compile the complete unity
compiler for `aarch64-linux-gnu`.  The benchmark defines `BUSTER_SANITIZE=1`
only to exclude the host-ucontext crash handler whose `pc` spelling belongs to
the host headers; it otherwise selects all 3,851 supported functions and
1,236,894 machine rows.  Every old/new 47,403,832-byte AArch64 ELF is
byte-identical (SHA-256
`d08fe6dfe1113ce53d122e2af693f8ddd4b27f8e47e0f2e9f4bc0d4a7f98a346`).
Retired instructions fall 9,728,495,385 -> 9,710,475,915,
**-18,019,470 (-0.1852%)**; branches fall 1,830,821,855 -> 1,822,550,436,
**-8,271,419 (-0.4518%)**; branch misses fall 22,092,499 -> 21,748,216,
**-344,283 (-1.558%)**.  The independent cycle medians move 3,966,576,216 ->
3,968,821,006 (**+0.0566%**), within run-to-run noise and not a speedup claim.

A second five-pair run uses the generated `tests/c_abi_cfuncs.c` ABI corpus
without the unity-header workaround.  All 459,288-byte AArch64 objects are
byte-identical (SHA-256
`045756f7d9db38777871ced234872d609b28c71ac9965128c5ff03c4d6f7018c`):
instructions fall 108,756,579 -> 108,219,541 (**-537,038 / -0.4938%**),
branches fall 19,625,761 -> 19,461,686 (**-0.8360%**), and branch misses fall
148,263 -> 145,314 (**-1.989%**).  The complete unity compile remains the
primary end-to-end measurement; this independent corpus confirms the target
selector's direction on a different function population.

The existing selection-order differential test deliberately relinks two
independent rows and runs for AArch64; it passes through the gathered fallback,
while the ordinary corpus exercises the dense path without a per-row ID
allocation.  Release
`test_all` passes 309,491 assertions in all 39 modules and sanitized Debug
passes 307,613 assertions in all 39 modules.  The final `test_self_host`
reaches a byte-identical 35,961,328-byte fixed point: stage 1 retires
9,871,486,709 instructions and stage 2 retires 72,724,018,602.  The native
x86-64 self-host does not execute the changed selector, so its small increase
over the pre-change 9,869,318,712 / 72,706,220,438 baseline is added source
cost, not a native throughput claim; the measured win is the same-source
AArch64-target compile above.
