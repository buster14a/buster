`2026-08-18a` (Linux x86_64, Zen 4 7940HS; **exact-recipe scratch,
selector storage reuse, and dead debug traversal**). The post-17h trusted
Release profile still centers native code generation on
`buster_x86_metadata_emit_exact_machine` (6.47% self cycles), FAST placement
(4.66%), `machine_encode_x86_64` (4.47%), generic metadata scratch emission
(4.41%), `machine_select_canonical_function` (3.81%), metadata binding
(2.33%), FAST prepass (1.29%), and minimal selection prepass (0.69%). Branch
and L1d misses remain secondary to retired scalar work; this round therefore
keeps only changes that reduce the stable instruction gate.

The x86 exact-recipe and exact-sequence emitters no longer clear their
four-operand transactional scratch arrays. Every active operand is assigned by
the projection switch before a successful metadata call; invalid payload,
slot, alternate-token, and projection cases return or short-circuit before the
array can be read. In isolated seven-round gates with byte-identical outputs,
the recipe change reduces 14,803,665,622 -> 14,797,248,106 instructions
(**-6,417,516, -0.0434%**) and the sequence change reduces 14,792,430,342 ->
14,787,717,009 (**-4,713,333, -0.0319%**).

The x86 and AArch64 selectors reuse the target-local `local_store_counts`
array for branch-fusion store ordinals after the alias sweeps consume its last
count. The existing ordinal initialization remains, and the two lifetimes do
not overlap. This removes one value-count-sized arena allocation per selected
function. The isolated median is 14,793,969,508 -> 14,793,663,495
(**-306,013, -0.0021%**); all seven paired deltas are negative.

Debug-model construction removes a visited/worklist DFS whose roots were
already every copied type and whose result and bounds observations were never
consumed. Type copying and all model output remain unchanged. The isolated
median is 14,795,268,280 -> 14,793,643,034
(**-1,625,246, -0.0110%**) with byte-identical objects.

The clean merged-main versus final-candidate cumulative A/B uses seven
interleaved non-multiplexed rounds per instruction/branch/L1d group and nine
cycle rounds, with byte-identical output in every pair. Median instructions
are 14,800,200,081 -> 14,787,714,912, **-12,485,169 (-0.0844%)**;
branches 2,728,429,525 -> 2,726,949,232 (**-0.0543%**); L1d loads
5,327,049,423 -> 5,298,231,725 (**-0.5410%**); and L1d misses
136,089,530 -> 135,661,217 (**-0.3147%**). Branch misses move
27,961,969 -> 28,046,223 (**+0.3013%**). Nine cycle pairs are mixed: sorted
medians move +0.325%, while the paired median is effectively flat at +0.016%.
No cycle claim is made; the changes are retained for the stable instruction,
branch, and cache-traffic reductions.

Aliasing an `ObjectFile` text section directly to `CodegenModule.code` was
left untaken. In-tree driver, linker, writer, assembly, and JIT consumers only
read the section while the module arena remains alive, but the public
`object_from_canonical_codegen_module` API does not promise a borrowed module
lifetime. Removing its unique text copy would silently make external callers
retain the module storage, so this needs an explicit ownership contract or a
driver-private path rather than a local throughput patch.

Final `test_self_host` reaches a byte-identical 35,500,368-byte fixed point:
stage 1 retires 14,779,410,907 instructions and stage 2 retires
103,833,432,232, and all 1,440,472 exact attempts succeed. Release `test_all`
passes 309,175 assertions across 39 modules. The complete local combination
matrix also passes: Clang unsanitized and sanitized Debug/Release, GCC and Zig
Debug compilation, the canonical 309,177-assertion table-audit run, and Clang
static analysis with zero warnings.
