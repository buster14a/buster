`2026-09-11T200359Z` (Linux x86-64, hosted Intel Xeon, Clang 18.1.3,
coordinated quiet window; **Same-binary canonical FAST opt-in throughput
diagnostic**).

## Revision and method

This follows the correctness/work-population record
[`2026-09-11T193052Z`](2026-09-11T193052Z.md); it does not amend that record.
Measured source: local `7b397f689f5b040abdb1f97926360f199a22bee5`, published
`c837e00599a2503f6dbe94735533c7c6e3d1841f`, identical tree
`f572a5947e3ff5303126fd414f81469f29348c9c`. The Clang 18.1.3 Release compiler
uses non-unity compilation, tests/debug information/frame pointers enabled,
allocation instrumentation disabled, and the repository's effective `-O2`.

Both variants execute the **same** 39,375,296-byte trusted compiler, SHA-256
`6c8dd8dd530dd37c10bd9b399bafdeb9bd64a653868fef92c26651a3efe283cf`.
Its hash was unchanged before/after timing. Small shell wrappers append
`-fno-canonical-fast` or `-fcanonical-fast` only to `cc` commands; all other
arguments are identical. The harness records wrapper hashes, so the separate
[provenance record](evidence/2026-09-11T200359Z/provenance.json) also preserves
wrapper contents and the actual compiler hash. The common wrapper launch is
included in measured process time.

All agents stopped local builds, tests and other benchmarks for this run.
This is a coordinated quiet hosted-machine window, **not** a qualified
physical acceptance host or an A/A noise calibration. CPU 8 affinity was
requested. The fixed `ci` profile uses seed 20260907, all six workloads and
four allocator modes, two warmups per variant, and two rounds of 20 adjacent
pairs per case. Every one of the 1,920 timing samples was retained. No PMU or
allocation instrumentation ran on the timing path.

```sh
build/throughput-tools/throughput run \
  --baseline /workspace/scratch/938e5009f152/issue40/build/fast-throughput-off \
  --candidate /workspace/scratch/938e5009f152/issue40/build/fast-throughput-on \
  --output build/fast-throughput-on-off --profile ci --mode all \
  --pairs 20 --warmups 2 --cpu 8 \
  --baseline-id c837e005-fast-off --candidate-id c837e005-fast-all
```

## Results and decision

The guard returned exit 0: **zero confirmed substantial regressions,
22 inconclusive cases out of 24**. Its thresholds are wall time above both
15% and 2 ms, or RSS above both 20% and 16 MiB, significant in both rounds.
An exit 0 with these intervals does not demonstrate equivalence or improvement.

Selected all-enabled paired wall-time changes were +7.38% for control flow in
FAST mode, +6.63% for backend pressure in FAST mode, +4.79% for control flow in
QUALITY, and +4.12% for the large function in QUALITY. These unfavorable
observations are retained, including all rounds and outliers. Peak RSS medians
were close in this series. See the [complete 24-case summary](evidence/2026-09-11T200359Z/summary.md).

The result does **not** justify default adoption. All four passes remain
opt-in; #40's end-to-end acceptance remains unresolved. The next bounded
investigation is avoiding irrelevant opcode/type/constant queries and comparing
that candidate with this frozen binary, followed by independently disabled
pass measurements. Any revised implementation requires a separate record.

## Evidence and correctness

[Raw run archive](evidence/2026-09-11T200359Z/run.tar.gz) preserves all samples,
commands, per-invocation capability observations, metadata, fixed input files,
host observations and statistical summaries. Generated object artifacts are
excluded; their sizes and hashes remain in every observation. Expand the
archive to inspect/replay its statistical data. The compiler and generated
objects are reproducible from the recorded source/build/command provenance.

The measured revision includes CI fixes for GCC's struct `TimeDataType` and
mobile platforms without subprocess execution. The mobile test adjustment
passed the complete local Clang Debug suite (1,486,994 assertions, 46 modules).
The timing fix compiled the full `ir.c` translation unit with GCC using
`-Wall -Wextra -Werror -Wconversion`; the trusted Release rebuild succeeded.
New-head platform CI was still running at record time. Earlier all-pass and
default fixed points and cross-backend execution are recorded in the preceding
audit; they are not represented as fresh checks of this follow-up revision.
