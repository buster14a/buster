`2026-09-09T002241Z` (Linux x86_64, Intel Core i7-1185G7; **Preserve AT&T
literal mnemonics and typed suffix widths (#193)**).

This recovers the prepared #193 suffix fix from the task
[Tackle Problems 190 195](https://chatgpt.com/c/6a9eb3a5-2aec-83eb-b210-bdf7794ea878)
and `origin/agent/inspect-190-195`. The candidate is based on
`9f5616613b4c1f1af4b4071a2cbe987db20490b8`.

`assembly_x86_metadata_suffix_alias` formerly treated every final `s` as a
32-bit suffix and every final `t` as an 80-bit suffix. An invalid unsized
`bts $3,(%r23)` path could therefore retry `bt` and silently emit another
operation. Invalid `adds`, `cmps`, and `movs` integer operand forms could also
reach shorter integer mnemonics.

The recovered change restricts generic widths to `b/w/l/q` and obtains `s/t`
only from existing typed aliases. A complete literal metadata mnemonic keeps
its identity. The original blanket exact-name guard was adapted: registered
scalar `movq` remains a MOV alias even though the distinct MOVQ metadata family
owns the same source spelling for MMX/SSE operands. x87 real/integer aliases
and the doubled `ll` integer forms retain their typed widths. Metadata remains
the final byte authority.

The source regression contains 41 GNU as 2.47 byte/rejection cases: 24 valid
integer, MOV and x87 controls, eight invalid suffix forms, and nine unsized
bit operations that must either match the oracle or reject the operands. The
unchanged compiler emits wrong bytes for six BTS cases and accepts four of the
invalid spellings. The candidate passes all 41. APX bytes are checked as object
bytes, not executed on this Tiger Lake host. A pre-existing XMM-to-XMM MOVQ
operand rejection was excluded from this issue's regression; preserving MMX
MOVQ and scalar MOVQ is checked explicitly.

A complete record comparison of the 11,013-form census changes exactly two
rows: 4405 / `9af889b056df9703`
(`VCVTTSS2SIS_GPR32i32_MEMf32_AVX512`) and 4414 / `2ba3c95989be18db`
(`VCVTTSS2USIS_GPR32u32_MEMf32_AVX512`). Both move from AT&T byte mismatch to
invalid operands when the erroneous generic `s` fallback is removed. The two
new classes and stable hashes have explicit assertions. All 5,737 exact AT&T
rows remain exact; Intel counts and the structural ledger are unchanged.
The first full runs failed only the two old aggregate census expectations;
those expectations were refreshed only after this record-level comparison.

Validation uses the trusted Clang-built compiler:

- Before edits, the complete Release self-host passes with a byte-identical
  39,903,752-byte stage1/stage2 fixed point, including its supplemental stages.
- The non-unity Release suite passes 901,509/901,509 assertions and 44/44 modules.
- The strict-sanitizer Debug suite passes 897,230/897,230 assertions and 44/44
  modules, with no ASan or UBSan diagnostics.
- The independent GNU as 2.47 source oracle passes 41/41 candidate cases.

No compilation-throughput or generated-program speedup is claimed. The
inherited `tools/new_audit.py --check` failure for the unchanged historical
`2026-08-24T131821Z` entry is outside this slice.
