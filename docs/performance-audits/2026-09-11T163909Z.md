`2026-09-11T163909Z` (Linux x86-64, Intel i7-1185G7, Clang 22.1.8; **Remove
dormant machine descriptor state**).


## Scope and exact revisions

Baseline main: `f52360b20f73eb9258516687947dabddfa4f76f6`.
Measured code/test commit: `ce8b9242d54046d6d6e050e9ba112d26b0315b93`.
The same focused patch was subsequently rebased without conflicts onto
`3d35b5d39ea31de04f9813996d241cf53914b65f` (merged #427/#428), yielding
`5bdfcec1caddd68cab8b98c7af4569b778d5e6be`. Measurements below precede that
integration and do not measure the AArch64 conversion additions. Final-head
validation must be attached to the PR separately; this audit is immutable.

The cleanup removes nine unconsumed descriptor fields, their initializer
assignments, three unused helpers and the bundle enum. It retains all active
operand, constraint, clobber, memory and vector-state facts and the separate
recipe projection. Explicit padding keeps every retained offset and the
96/24/16-byte descriptor/instruction/projection strides. The descriptor table
remains 246 * 96 = 23,616 bytes: **zero table-byte saving**. No production
allocation, pass or projection is added. #45 remains open for the remaining
field census and overlapping authorities.

## Reproduction and equivalence

Both measured binaries use the normal local TCC-bootstrap Release build,
Clang 22.1.8, O3, native CPU selection, debug information and frame pointers.
Exact binary hashes, sizes, inputs and command lines are retained in
[metadata](evidence/2026-09-11T163909Z/metadata.json) and the
[command log](evidence/2026-09-11T163909Z/commands.jsonl).

A temporary diagnostic C program extracts the actual `MACHINE_OPERAND_*`
macros and `machine_opcode_infos` initializer from each revision's
`machine.c`, includes `machine.h`, and prints every retained semantic field
for all 246 rows (including every slot's operand/register byte and name).
Clang compiles that static-table diagnostic; it is not a compiler-bootstrap
substitute. Both dumps are identical, SHA-256
`47f13404300db35221a4031c369299e03e909d6cc85fe0045178b98e0b59ac2d`.
The [retained records](evidence/2026-09-11T163909Z/retained-metadata.txt) and
[source/layout evidence](evidence/2026-09-11T163909Z/source-evidence.txt)
are attached. Existing-suite tests add full-domain clobber projection/flag
checks and independent DIV/MULH/conversion scratch-register expectations.

After the local compiler build and tests finished, run:

```sh
./build.sh bench_throughput run --baseline ../next45-baseline-ide --candidate ../next45-candidate-preintegration-ide --output ../next45-throughput --profile smoke --mode all --pairs 8 --warmups 1 --no-guard --require-identical-output --baseline-id f52360b20f73eb9258516687947dabddfa4f76f6 --candidate-id ce8b9242
./build.sh bench_throughput compare --output ../next45-throughput
```

All 768 observations (384 pairs, six workloads, four modes, two rounds) are
retained in [samples.csv](evidence/2026-09-11T163909Z/samples.csv); all paired
outputs are identical. The [complete timing/RSS table](evidence/2026-09-11T163909Z/summary.md)
and [statistical replay](evidence/2026-09-11T163909Z/summary.json) preserve all
observations, including increases. FAST baseline/candidate median wall times
in milliseconds: startup 12.884/12.838, large function 12.936/13.041,
many functions 13.145/13.027, symbols 12.797/13.555, control flow
15.572/15.692, pressure 20.496/20.486. Peak-RSS medians vary in both directions;
the full table reports them, with no inferred allocation reduction.

This is an active Intel i7-1185G7 desktop with eight logical CPUs and SMT,
without pinned affinity. No task-owned build, test or other benchmark ran
during the timed observations. PMU and allocation replays were not requested.
The smoke scale and disabled guard make this diagnostic only: the harness's
zero confirmed/inconclusive counts do not establish equivalence or acceptance.
No speedup, Zen 4/5 result or compiler-build-time improvement is claimed.
A controlled repeated compiler-construction measurement was not collected.
The trusted Release executable grew from 38,075,088 to 38,077,408 bytes,
including the added embedded tests and debug data; this is not a runtime-RSS
measurement or an isolated production-code-size effect.

## Correctness and remaining gates

The unchanged baseline passed ordinary `./build.sh test_self_host --config
Release`, including the byte-identical 38,779,592-byte fixed point and the
MIR_STACK/NONE gates. The pre-integration candidate passed
`./build.sh build --config Release -t test_all`: 1,483,610 assertions,
46 modules, zero failures (505 additional assertions). Layout assertions and
`git diff --check` pass.

At audit creation, full local Release, ordinary self-host, all allocator/target
modes, strict Debug ASan/UBSan and hosted platform/bootstrap/throughput checks
for the rebased head are pending. They are not claimed green here. The audit
records the cost and evidence of this bounded cleanup, not completion of the
full #45 acceptance criteria or any #36/#37 retirement/register-bank work.
