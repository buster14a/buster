`2026-09-10T010600Z` (source inspection; runtime and hardware evidence pending
CI; **QUALITY scratch census through existing diagnostic replays; no
optimization claim**).


Baseline: `ef99ec72cf0f17c8818a1d4b4c106fd5908b5eae`.
Classification: verified diagnostic coverage gap, not a reproduced allocation
correctness bug or measured performance opportunity. Scope is #125 under #128;
#298 and #311–#313 are untouched. The current lazy prefix construction is already
completed work and is observed rather than reimplemented.

Adds a bounded, compile-time-optional census to the existing allocation replay
and source-metrics path, plus counter and dirty-scratch/ordered-placement controls
in the existing machine module. See `docs/quality-scratch-census.md` for all fields,
precise scopes, exclusions and interpretation. No machine/IR rows are widened,
no second timing stack is added, and no placement/order/ISA policy changes.

The pinned source and paginated GitHub evidence are retained in CI run
https://github.com/buster14a/buster/actions/runs/34423551451 (read-only export, not a
compiler test). Its inventory covered 142 open issue/PR rows and five open PRs;
#362 overlaps only the machine test file. Relevant closed searches and current
file history were inspected. Historical Forgejo references were checked against
the migration map/archive; current GitHub #125/#128/#298/#311–#313 are not remapped.
The recovered closure experiment's one-region loss is not retried here.

At authoring, no local compilation or tests have been run, by task policy. Full,
mode, platform, self-host and enabled-diagnostic validation is required in CI on
the submitted SHA. Actual CI and raw sample links belong in the PR report, not a
claim that this source-inspection entry measured execution. No CPU identity,
Zen 5/Zen 4 nonregression, PMU values, phase timings, RSS improvement or speedup is
claimed. Diagnostic observer cost must never be presented as production latency.
