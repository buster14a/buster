`2026-08-22T203845Z` (Linux x86_64, Zen 4 7940HS; **the window lexer's three
standing census leads taken together: carry-propagated number extents,
register-blended punctuator ids, and sixteen-row token stores (stage 1 -30,0 M
instructions)**).
`2026-08-22T140940Z` left three `c_lex_compact` leads standing at single-digit
M each, below the bar individually.  This entry takes them as one composite —
the three restructure the same window body, so their fixed costs share — and
the composite lands at nearly double the bar.

**(1) Number extents by carry propagation.**  The forward-seeking cursor
resolved every preprocessing number serially — 620 K iterations, 93% of all
cursor work.  Numbers are now resolved all at once, for every window whose
extent candidates are number seeds only (no quote, comment open, apostrophe
or ellipsis anywhere in the window — the classes whose precedence is what the
cursor's left-to-right order exists to arbitrate; such windows keep the
cursor).  Two additions do the whole window: the first seed of each
continuation run is `((continue & ~seeds) + run_starts) & seeds` — run starts
ripple through the continuation bytes below the first seed and land on it,
while later seeds (the digit in `5.3` or after `1e+`) sit on cleared sum bits
— and the spans are `((continue + first_seeds) ^ continue) & continue`, the
bits the carry flipped on its way through each run.  A span reaching lane 63
mid-file peels off as the deferred item, exactly as the cursor would have
deferred it.  The formulas were brute-forced against the serial semantics
over 2 M random windows (EOF-shortened ones included) before landing: zero
mismatches.

**(2) The punctuator ids stay in registers (2026-08-10b's five-blend lead).**
The emitter stored the singles id vector to a stack array, let the
maximal-munch walk patch pair/triple/quad ids into it byte by byte, and
reloaded the array as a vector — a store-queue round trip on every window
plus a table walk per clustered punctuator.  The ids now blend in registers:
pair ids come from the same 16x16 cross-product table the walk indexed,
addressed by vpermi2b row/column key lookups on the two bytes
(`row << 4 | column`, the 256-entry table as four zmm with a movepi8 select
on the index's high bit); triples index their first-byte table; the quad is
one set1.  Ids land on every candidate lane and the starts mask selects the
real ones.  The walk itself shrinks to starts-only mask arithmetic and runs
at all only when a pair/triple/quad candidate intersects the operator set —
a run of single-byte punctuators (`));` and friends, the common case) is its
own starts mask with no loop and no blends.

**(3) Sixteen-row token stores.**  The 8-row store spent lanes 8-15 of the
offset vector as its zero source for the symbol dwords, so a pass covered
eight rows and the mean 11-token window paid two passes plus four lane
rotations (27,1% of stored rows were tail slack).  The zero dwords now come
from the interleaving permutes' own zeroing masks (`maskz vpermi2d`, three
index patterns over offsets+metadata), so one pass widens sixteen tokens and
stores 192 bytes; 76% of emitting windows fit in that single pass.  Passes
past sixteen rotate by four dwords with `valignd` instead of a vpermb with a
loaded index vector, and the token array's tail slack grows 8 -> 16 slots.

**Counters, 11 alternating same-source CPU-pinned pairs, outputs
byte-identical every pair:** stage-1 instructions 9.871.734.439 ->
9.841.757.638 (**-29.976.801, -0,3037%, 11/11**, spreads 0,003%), branches
1.818.654.580 -> 1.815.050.058 (**-3,60 M, -0,1982%, 11/11**).  Cycles favor
the candidate -0,38% at 5/11 with ~40% spreads and branch misses read +1,99%
at 4/11 with ~17% spreads — both unresolvable noise at this size (a
concurrent session was building throughout; `2026-08-22T140940Z` and
`2026-08-19c` record the same verdict for these counters).  The baseline
median is 9.871,7 M against the census entry's 9.970,2 M reference because
PRs 511-521 landed in between; the fixed point moved with them, to
`SELF_HOST deterministic bytes=35956504` — the output is the compiler's own
source, and this tree's source is bigger than PR 510's.

**Gates:** codegen-neutral (baseline and candidate binaries over the
identical tree, same `-o`, byte-identical output), self-host deterministic,
all 309.490 Release unit tests in 39 modules pass — the differential gate's
construct cases, corpus-at-every-phase and fuzz blobs now compare the carry
formulas, the blends and the 16-row stores against the scalar reference —
`-march=x86-64 -fsyntax-only` clean under the full GNU-family warning set
(the compact path compiles out; the scalar reference is untouched), the
`nm | grep -c ' t _mm512_'` optnone probe reads 0 on both quoted binaries,
and the local matrix is green.

- **The composite is why this cleared.**  Separately the census bounded
  these at ~10 M (cursor numbers), and single-digit M each (store tail,
  punctuator round trip); together they measured -30,0 M — the shared
  restructuring (one window body, one set of masks, fewer live vectors) let
  each lever keep more of its headline number than the per-lead accounting
  predicted.
- **Left standing, still ranked by the census:** window re-classification
  overlap (23% of loaded lanes, inherent to item-aligned windows), and the
  mixed-window cursor (the ~7% of iterations that are comments/literals plus
  whatever number share lands in windows that also hold a quote or
  apostrophe).  Neither has a visible mechanism below several M.
- **Do not retry** (unchanged from `2026-08-22T140940Z`): in-chunk escape
  parity for the literal find; "lexer writes the preprocessor's final slots".
- Reference points for the next audit, clang-built on this host: stage 1
  **9.841,8 M** instructions / 1.815,1 M branches (this tree, unity `ide.c`,
  `-g`), fixed point `SELF_HOST deterministic bytes=35956504`, Release
  `ide test` 309.490 assertions in 39 modules, token-array slack 16 rows.
