`2026-09-10T191117Z` (Linux x86-64 KVM guest, Clang 18.1.3 and GCC 13.3.0;
**Z16: recover explicit object and assembly measurement through the existing
harness**).

This recovers the offline `z16-assembly-measurement.patch` associated with #116
and campaign #390. It is the measurement prerequisite, not the unimplemented
assembly-printer index. The source is stacked on #401 at
`874976ee56b5a1eaa12be1caa182a7bdef868de5`, preserving schema-2 process
diagnostics and #400's stale-summary/write-failure behavior.

## Behavior

The preserved patch's actual option is `--artifact object|assembly`; early
issue coordination called the proposal `--emit`. The implementation, help and
README consistently use `--artifact`. Object remains the default and preserves
`-c` and `.o`. Assembly chooses `-S` and `.s`, marks ordinary series with
`/assembly` and records artifact kind in metadata. Frozen self-host jobs remain
executable compiler builds in either setting. Existing `--flag` restrictions
reject `-S`, `-c` and `-E` so flags cannot silently change operation.

No compiler source, work generator, result schema, statistical family, sample
order/count or threshold changes. This introduces no second launcher. PMU and
allocation diagnostic replays reuse the selected operation and preserve their
output-identity checks. All later census documentation and tests remain present.

## Validation

Clang 18.1.3, GCC 13.3.0 and strict Clang ASan/UBSan each pass **30 core and 602
integration assertions**. Builds use `-std=c11 -O2 -Wall -Wextra -Werror
-Wpedantic -fwrapv -fno-strict-aliasing -funsigned-char` and link `-lm`.
The sanitizer run additionally uses `-g -fsanitize=address,undefined
-fno-sanitize-recover=all`, `UBSAN_OPTIONS=halt_on_error=1` and local
`ASAN_OPTIONS=detect_leaks=0`; hosted leak checking stays enabled. This
environment does not support local leak inspection.

Command tests cover both artifacts, four allocators and all four stage values,
with exactly one ordinary operation, no preprocessing, correct artifact
extension, executable self-host paths, preserved optimization/allocator order,
and invalid/missing selections. Independent comparisons verify all six input
files against #401 in each final build (18 byte-identical file comparisons).

Two real compiler A/A collections use the same frozen local compiler executable,
SHA-256 `d514e6165ce585fdd5677772f8e31cb558fb6adb98c8d8377dc54b1b55df0544`,
on both sides. It was copied before collection from the completed local #366
test build; this artifact hash, not an inferred source identity, identifies the
control. No compiler change or baseline/candidate optimization is measured.
No other local compiler build/test/benchmark was observed at collection start.

Each collection uses:

```sh
throughput run --baseline /absolute/frozen/ide --candidate /absolute/frozen/ide \
  --output /new/results --artifact object --profile smoke --mode all \
  --pairs 2 --warmups 1 --no-guard --require-identical-output
```

Repeat with a separate output directory and `--artifact assembly`. Both bundles
complete with 24 workload/mode combinations, 192 valid samples and 240 recorded
commands including warmups. Repeated/A-B outputs match exactly. Metadata, series
names and all 480 argv/output paths independently match the selected operation.
Both replay results are valid with `guard_enabled:false`; two pairs are a
functional smoke control, not statistical acceptance or performance equivalence.
No timing values from these controls are offered as a performance result.

`git diff --check` passes. The audit index checker retains the historical
opening-format error for `2026-08-24T131821Z`; no historical audit is rewritten.
Final submitted-head platform/harness/aggregate CI and merge identity are
reported in the PR. #116 remains open for production symbol/relocation indexing,
independent assembly correctness and representative end-to-end/scratch
acceptance. Physical Zen 5/Zen 4 gates remain under #46/#128.
