`2026-08-22T205418Z` (Linux x86_64, Zen 4 7940HS; **the AArch64 machine
backend's fifth installment — vector literals and chunked NEON arithmetic,
atomics, ELF thread locals, dynamic stack allocation, SWITCH terminators,
and the sub-sixteen-byte vector ABI take the census from 790 to 962 of
990**).

The `2026-08-22T150012Z` plan executed on a corpus that had tripled under
it: the Zig `test/c_abi` suite port (`tests/c_abi_*`, PR 516) landed on
main the same day, so the re-measured census stood at **790 of 990**
fixture functions (FAST, aarch64-linux) against T150012Z's 252 of 294 —
200 fallbacks, three quarters of them the suite's vector literals
(ARRAY 76), reopened tiny-vector signatures (47-class 58), and their
calls (CALL 19). Census and qemu-differential harnesses re-derived from
the T124313Z reproduction paragraph as always; the differential grew a
c_abi lane (the three translation units compiled whole per allocator
mode and run under qemu-aarch64, with a local no-op `write()` stub TU so
the static link holds — the panic path is the only caller).

**Fixture-corpus machine coverage (FAST, aarch64-linux): 790 to 962 of
990. Signature rejections 58 to 0, CALL 19 to 0, ARRAY 76 to 0, BINARY
5 to 0, atomics 6 to 0, GLOBAL/TLS 4 to 0, STACK_ALLOCATE 4 to 0,
SWITCH 2 to 0.** qemu differential 43 fixtures x 4 allocator modes plus
the c_abi trio, 0 mismatches at every commit; `ide test` Release and
Debug green at every commit (309538 unit assertions, 39/39 modules, at
the close); `test_all_combinations_ci` green before each push.

- **Vector literals (ARRAY 76 to 0).** The canonical emitter lowers
  IR_OPCODE_ARRAY for arrays and vectors through the same per-element
  copy loop; the machine array path only accepted IR_TYPE_ARRAY.
  Accepting IR_TYPE_VECTOR was the entire change — lanes are always
  1/2/4/8-byte scalars the member write already stores sized.
- **Vector arithmetic (BINARY 5 to 0), the T150012Z-named lever.** One
  new row, MACHINE_A64_VARITH: the NEON three-same word per sixteen-byte
  chunk through the fixed V0/V1 compute scratches (VLOAD_FRAME both
  operand chunks, compute, VSTORE_FRAME the result), riding the
  scheduler's float-state chain — no vector register file, exactly as
  predicted. Only the NEON-encodable set selects (add/sub/mul, the
  bitwise trio, float add/sub/mul/div; no 64-bit-lane multiply);
  divisions, remainders, shifts, and comparisons keep the canonical
  per-lane path, which parks `basic_c_vector.c`'s main on its vector
  remainder as the recorded UNARY 1 residue.
- **Atomics (6 to 0).** Loads and stores as single ldar/stlr words over
  ordinary vreg operands (every memory order takes the one-word form);
  the read-modify-write and compare-exchange exclusive loops as
  constrained bounded sequences on the canonical emitter's own X9-X13
  palette, declared through `fixed_register_mask` exactly like VA_ARG,
  loop words lifted verbatim; dmb ish behind the same signal/relaxed
  suppression. All five follow the x86-64 precedent's SIDE_EFFECTS
  barrier attribute.
- **ELF thread locals (GLOBAL 4 to 0).** MACHINE_A64_LEA_TLS: mrs
  tpidr_el0 plus the TPREL_HI12/LO12 add pair with the destination
  register free, Linux/Android only (Darwin keeps the canonical
  tlv-call path). MachineCallSite grew `thread_local_low` to tell the
  pair's relocations apart; the driver test's resolved-TPREL probe now
  matches the pair's form on any register instead of the canonical x9
  word.
- **Dynamic stack allocation (STACK_ALLOCATE 4 to 0).** One constrained
  row — count in through X9, aligned pointer back in X10 — expanding
  the canonical page-probed loop whole in the encoder, X9's destruction
  by the countdown declared per the x86-64 xchg lesson. The new
  contract: a selected allocation moves SP below the fixed outgoing
  argument area whose base every call with stack parts reads as its own
  stack pointer, so finalize rejects the pair whole (which calls need
  the area is only known once every call has selected).
- **SWITCH terminators (2 to 0).** The x86-64 shape ported exactly: one
  terminator row over the shared switch-case side table, the target
  description's `switch_opcode` set so terminators classify
  structurally, and the encoder expanding the canonical chain — case
  constants in reserved X17, full 64-bit compares, equality branches
  through the ordinary block-fixup machinery. One matrix lesson paid:
  gcc's -Wtype-limits rejects a tautological u16 bound the clang lanes
  accepted, which is what `test_all_combinations_ci`'s gcc lane is for.
- **The sub-sixteen-byte vector ABI (signature 58 to 0, CALL 19 to 0).**
  T124313Z's "eight-byte short-vector form stays canonical until the
  corpus grows one" met a corpus that grew dozens. AAPCS64 hands a
  1/2/4/8-byte vector the low bytes of a V register, so the shape gate
  accepts the sized VECTOR parts (non-power-of-two lane counts stay
  canonical) and one helper picks the q rows or the new sized pair
  (VLOAD/VSTORE_FRAME_SIZED, scalar FP b/h/s/d words verified against
  llvm-mc) at all four ABI edges — entry capture, call staging,
  call-result receive, return. Placement was already size-agnostic: one
  float part per vector, the V-file overflow quirks untouched. The
  c_abi trio's whole tiny-vector interop matrix now executes
  machine-selected in fast/quality against the canonical oracle.
- **Encoder capacity is per-row, not shared slack.** The atomic loops
  initially rode the 12-byte default capacity row and survived on the
  edit-count slack — luck, not a bound; every encoder-expanded sequence
  now carries its own capacity case.
- **One matrix run discarded.** A src edit landed while a
  `test_all_combinations_ci` run was still compiling the live tree (the
  T150012Z lesson, paid again); the run's green was discarded and a
  clean re-run gated the push.
- **Left untaken.** Inline assembly 10 (dedicated session); label
  addresses 8 + INDIRECT_BRANCH 1 + the computed-goto ARGUMENT 1 —
  measured equally unsupported on the x86-64 machine path, so this is
  cross-target design work (ADR-class relaxer surface plus
  indirect-edge contracts), not an a64 catch-up item; i128, whose
  visible residue is bodies (LOAD 4 + CAST 2, from the suite's
  struct-wrapped `__int128` members — the x86-64 pair model is the
  precedent); the vector-remainder UNARY 1. **The signature 0 carries
  one caveat worth the next session's time: bare `__int128` parameters
  and returns are still rejected — probed directly, while a
  struct-wrapped one selects — and the census cannot see them because
  `ZIG_NO_INT128_SHIFTS` gates the suite's bare-i128 pair
  (`zig_big_packed_struct` and friends) out of the aarch64 run
  entirely. That gate's own comment claims 128-bit passing and
  returning "stay tested", which on aarch64 they do not. Lifting it
  reopens the signature class, so the i128 batch is signature work plus
  bodies, not bodies alone.** Stage-11
  additions this session, all with host-convention-safe native typing:
  vlit_make, vquad_add, vf4_scale, amix, vla_fill, pick4, v8_pass,
  v2_make. Recipe-registry expansion count 62 to 73.
- Reference points for the next audit: fixture-corpus machine coverage
  962 of 990 (13 target-gated/negative fixtures COMPILE_FAIL at
  aarch64-linux, expected), fallback classes {asm 10, LABEL_ADDRESS 8,
  LOAD 4, CAST 2, SWITCH 0, ARGUMENT 1, INDIRECT_BRANCH 1, UNARY 1},
  qemu differential 43 fixtures x 4 modes + the c_abi trio.
  Reproduction as in `2026-08-22T124313Z`, plus the c_abi lane: compile
  `tests/c_abi_main.c c_abi_main_generated.c c_abi_cfuncs.c` and a
  no-op `write(int, const void*, unsigned long)` stub TU together at
  `-target aarch64-linux` per mode and require a clean qemu exit.
