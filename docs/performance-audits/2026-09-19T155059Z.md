`2026-09-19T155059Z` (Linux x86_64, Ryzen 7 9700X; **Promotion discovery
removes its call prewalk; measured throughput change remains small**).

## Result

Summary-known promotion discovery now uses the sparse local projection and opcode summary. Conditional call qualification runs in the already-required operand/event walk, before any promotion, removing the remaining separate call-barrier row prewalk. Unknown-summary IR keeps the conservative discovery path. Candidate ordering and promotion statistics remain unchanged.

Independent item 3 of #791, PR #856. The median changes are small: cycles -0.1497%, wall time -0.0896%, and DRAM fills -0.1672%, with instructions +0.0851%. These ten pairs do not establish a robust throughput improvement; the scan removal alone is not evidence of a meaningful speedup.

All 22 warm/measured outputs and both full survey outputs were byte-identical. Every `IR_FAST`, `IR_LOCAL_PROMOTION`, `IR_LOCAL_PROMOTION_WORK` and `CODEGEN` summary line matched the baseline exactly.

| Metric | Base median | Candidate median | Candidate delta |
|---|---:|---:|---:|
| Wall time (ns) | 1,079,992,717.0 | 1,079,024,750.0 | -0.0896% |
| User cycles | 5,382,958,442.5 | 5,374,897,709.5 | -0.1497% |
| User instructions | 14,382,731,002.5 | 14,394,964,524.5 | +0.0851% |
| DRAM/IO fills | 12,617,782.0 | 12,596,684.0 | -0.1672% |

## Identities and method

- Frozen input and baseline compiler source: `2a9693b23b869461aab68835a1221dba2d8c3f1f`.
- Measured and fully validated candidate source: `8954cc9ba5120b49b6b0750fcd81dfaeb962eac6`; includes main `e013ef03b560677b1a6b47590e9eb05171b9b34d`.
- Base trusted Clang Release compiler SHA-256: `b315e6f10129c01b05463a0eef8e91f39d3af8a97b566327e5896c20bcc73386`.
- Candidate trusted Clang Release compiler SHA-256: `ebe975eea48c50116826897a0d745fe6376c3ba58b82d3293fce78057d5419ea`.
- Common generated executable SHA-256: `96c501c5c0f4ad74171635dfa15f755f39712ee5b7a8e0811869f4616c0f0965`.
- Host: AMD Ryzen 7 9700X (`benchpress`), Linux x86-64, Clang 22.1.8, canonical TCC 0.9.28rc bootstrap; complete identity in [host.txt](evidence/2026-09-19T155059Z/host.txt).

The user authorized direct Tailscale SSH access to the dedicated host. Runs held `~/bench/host.lock`, with no concurrent build or measurement, and pinned each compile to CPU 2. Both independently built compilers consumed the **same baseline checkout and generated headers**, with the same command:

```sh
COMPILER cc -Isrc -Ibuild/generated -DBUSTER_UNITY_BUILD=1 \
  -DBUSTER_INCLUDE_TESTS=0 -g -v src/buster/apps/ide/ide.c -lm \
  -o build/issue791-stage1
```

One unmeasured warm-up per compiler preceded ten paired measurements. Each pair reversed order (AB, BA, AB, BA, ...). `perf stat -x,` counted `{cycles:u,instructions:u,ls_any_fills_from_sys.dram_io_all:u}` together. Wall time used a monotonic nanosecond clock around that same perf invocation, including its common process overhead. These are medians of ten observations per leg, not a statistical equivalence claim. This focused diagnostic is not the separate native-retirement acceptance run.

Both legs also completed `tools/cache_miss_survey.py --dwarf --repeat 3 --cpu 2 --binary COMPILER`, from the frozen baseline checkout. The full reports retain per-symbol/source-line attribution. Those samples have skid and are used for attribution; the paired counters above own the comparison.

## Correctness and integration

The exact measured candidate passed `./build.sh test_self_host --config Release` and `BUSTER_TEST_JOBS=4 ./build.sh build --config Release -t test_all` on the 9700X:

```text
SELF_HOST fixed_point bytes=42563488 stage1=build/self-host/Release/ide-stage1 stage2=build/self-host/Release/ide-stage2
[2764654/2764654] Unit tests
[53/53] Module tests
```

The promotion fixtures additionally lower an independent copy with its opcode summary cleared, forcing the original row-scan discovery. They compare complete promotion statistics and the published instruction opcode/result/operand sequence, including ordinary, indirect and returns-twice calls, barriers and control-flow joins. The event walk resolves conditional call barriers before promoting any local; barrier functions do not contribute candidate-local statistics.

The affected native-retirement bindings were regenerated with the pinned dependency closure and SDK headers, then refreshed a second time with no change. The final binding check and external preparation passed. Local helper validation passed 15 rebind tests, 23 materializer tests and 55 contract tests, plus both helper self-tests. These checks validate bindings and harness contracts, not native-retirement performance acceptance.

The local Intel workstation reproduced the baseline fixed point, but its full suite has nine existing Windows/Wine mixed-vector failures on unchanged main. The completed local promotion-candidate run reproduced those nine failures. The dedicated 9700X full suites pass; no local failure is represented as green.

The final commit adds this audit and evidence only. PR CI covers that submitted revision separately; the measured compiler identity remains explicit.

## Retained evidence

- [Every paired sample, binary hashes and exact command](evidence/2026-09-19T155059Z/paired.json).
- [Identical compiler summary](evidence/2026-09-19T155059Z/compiler-summary.txt).
- [Baseline cache-miss/DWARF survey](evidence/2026-09-19T155059Z/base-cache-miss-survey.txt).
- [Candidate cache-miss/DWARF survey](evidence/2026-09-19T155059Z/candidate-cache-miss-survey.txt).

Raw perf captures and full build/test logs are retained under `/home/david/bench/issue791-20260919/evidence` on the authorized host. No old audit or closed index was edited.
