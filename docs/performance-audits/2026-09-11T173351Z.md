`2026-09-11T173351Z` (Linux x86-64, Intel i7-1185G7, Clang 22.1.8; **Make
memory effects the sole memory-chain authority**).


## Scope and provenance

Baseline main: `b4d56a359dc55343cfa91915e4025a26b3eecce7`.
Measured code/test commit: `41bfce4b896e7052e8208a959f16e88acc3b3e2e`.
This evidence-only commit follows that code. Both binaries were built with
the ordinary local TCC-bootstrap Release workflow, Clang 22.1.8, O3, native
CPU selection, debug information and frame pointers. Exact binary hashes,
input hashes, sizes and command lines are in the
[metadata](evidence/2026-09-11T173351Z/metadata.json) and
[command log](evidence/2026-09-11T173351Z/commands.jsonl).

The patch removes `MACHINE_OPCODE_ATTRIBUTE_MEMORY` and its producers,
leaving `memory_effect` as the sole static memory-chain classification.
The existing helper and publication boundary remain. No new production
pass, allocation, projection or lookup table is introduced. Calls, side
effects, terminators and schedule-class barriers retain their independent
ordering policy. Other attribute values and all descriptor offsets remain
unchanged. Descriptor/instruction/row strides stay 96/24/16 bytes; the static
descriptor table remains 246 * 96 = 23,616 bytes, with **zero byte saving**.
This is a bounded #45 authority cleanup, not closure of the full field census.

## Verified gap and retained classification

A temporary diagnostic extracts the actual `MACHINE_OPERAND_*` macros through
the end of `machine_opcode_infos` from `machine.c`, prepends the include of
`machine.h`, and appends the [census main](evidence/2026-09-11T173351Z/memory-probe-main.c).
Compile with `clang -Isrc -O2 -fwrapv -fno-strict-aliasing -funsigned-char`.
This static-table diagnostic is not a compiler-bootstrap substitute. Columns
are opcode, legacy bit, effect enum and the original combined predicate.

The [baseline](evidence/2026-09-11T173351Z/memory-baseline.txt) has 38 tagged
opcodes, 49 valid non-NONE effects, and zero attribute-only memory opcodes.
The [candidate](evidence/2026-09-11T173351Z/memory-candidate.txt) has zero tags
and the same 49 memory opcodes. Every opcode/effect/predicate triple matches;
the normalized text SHA-256 is
`85000b284766e2a73646cfaeb17d2eb03497d8a46ff1f702bec72788be52b0db`.
Source-wide reads confirm the helper was the duplicate bit's only consumer.

The existing frozen scheduler-membership regression checks both classifications
for all 246 opcodes. Additional controls protect the six READ_WRITE aggregate
copies, both READ incoming operations, and null/zero/invalid effects. The
alias-order regression uses that helper without redundant opcode exceptions;
its independent slot/range and conservative-copy ordering checks remain.
The ownership inventory now explicitly distinguishes diagnostic tracing from
allocator use for the remaining virtual-register field census.

## Paired diagnostic measurement

After all task-owned compiler builds, tests and self-host/mode runs ended:

```sh
./build.sh bench_throughput run --baseline ../next45b-baseline-ide --candidate ../next45b-candidate-ide --output ../next45b-throughput --profile smoke --mode all --pairs 8 --warmups 1 --no-guard --require-identical-output --baseline-id b4d56a359dc55343cfa91915e4025a26b3eecce7 --candidate-id 41bfce4b896e7052e8208a959f16e88acc3b3e2e
./build.sh bench_throughput compare --output ../next45b-throughput
```

All 768 observations / 384 pairs are retained in
[samples.csv](evidence/2026-09-11T173351Z/samples.csv). All paired outputs are
identical across six workloads and four allocator modes. The
[complete timing/RSS table](evidence/2026-09-11T173351Z/summary.md) and
[statistical replay](evidence/2026-09-11T173351Z/summary.json) include every
observation and increases as well as decreases. For example, startup FAST
median wall time is 21.323/18.056 ms, whereas startup QUALITY is
19.358/19.769 ms, baseline/candidate. These are observations, not isolated
causal effects or acceptance evidence.

This active Intel i7-1185G7 desktop has eight logical CPUs and SMT, without
pinned affinity. No task-owned build, test or other benchmark ran during
samples; PMU and allocation replays were not requested. Small smoke workloads
and the disabled guard make the experiment diagnostic only. The printed zero
regression/inconclusive counts do not establish performance equivalence.
No speedup or Zen 4/5 result is claimed. A controlled repeated Clang
compiler-construction measurement was not collected. The trusted Release
binary, including embedded tests/debug data, grew from 38,133,352 to
38,138,720 bytes (+5,368); this is not isolated production code size or RSS.

## Correctness and remaining validation

Ordinary baseline `./build.sh test_self_host --config Release` passed with
a 38,796,736-byte fixed point and MIR_STACK/NONE gates. Candidate commands:

- `./build.sh build --config Release -t test_all`: 1,484,217 assertions,
  46 modules, zero failures (511 added assertions).
- `CMAKE_BUILD_PARALLEL_LEVEL=2 BUSTER_TEST_JOBS=2 ASAN_OPTIONS=halt_on_error=1 UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ./build/build build --build-dir ../sanitized45 -t test_all`:
  strict Debug ASan/UBSan, 1,475,671 assertions / 46 modules, exit 0, leak
  detection retained. Reused the ordinary TCC-built driver and configured
  separate sanitizer tree to avoid overwriting a driver in concurrent use.
- `./build.sh test_self_host --config Release`: 38,796,696-byte identical
  stages, both SHA-256 `945c8ad046febed3a6d94eb8b5e10f9dad42e2169cd6fe228e6a45f5a5b1a67b`,
  plus MIR_STACK/NONE gates. Self-host timings use changed compiler sources
  and are correctness evidence, not paired speed measurements.
- `./build.sh test_mode_matrix --config Release`: 24/24 legs, eight executed
  and 16 oracle checks, no failures.
- `git diff --check` and audit-index validation pass.

At audit creation, hosted platform, repeated bootstrap and throughput gates
for the submitted head are pending. Exact final results belong in the PR;
this immutable audit does not claim an unrun gate or full #45 acceptance.
