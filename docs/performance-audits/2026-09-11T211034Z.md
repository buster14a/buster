`2026-09-11T211034Z` (Linux x86-64, hosted Intel Xeon, Clang 18.1.3,
coordinated quiet window; **Canonical FAST in the supported Release profile
without embedded tests**).

## Separate supported build profile

This final experiment measures source
`369877c8bb42094ea7a765b596a4a1fbaaf43ac2` in an ordinary optimized Release
build without embedded tests. No source, validation logic, transform mask
default, work limit, or guard threshold changes for this experiment. The
previous test-bearing experiments and their unfavorable observations remain
in [the preceding audit](2026-09-11T204614Z.md).

The existing macro is:

```c
#define BUSTER_IR_TRANSFORM_CHECKS (!BUSTER_OPTIMIZE || BUSTER_INCLUDE_TESTS || BUSTER_SANITIZE || BUSTER_VERIFY_IR_TRANSFORMS)
```

Preprocessing the actual Release `ir.c` command confirms `OPTIMIZE=1`,
`INCLUDE_TESTS=0`, `SANITIZE=0`, and `VERIFY_IR_TRANSFORMS=0`; a compile-time
probe fails if the expression is true. This is the supported production
policy already present in the measured revision. Uncertified callers still
validate both input and changed results. Debug, test, sanitizer and explicit
transform-check builds retain their mandatory changed-result checks. The
build driver's existing throughput-CI configuration also supports tests-OFF;
this experiment instead retains ordinary local Release settings so only the
embedded-test setting differs from the previously tested compiler.

**Provenance correction:** the preceding audit calls its ordinary Release
optimization level effective `-O2`. Its tested compiler's actual command is
`-O3`, `BUSTER_CI=OFF`, debug information/frame pointers enabled. This new
compiler uses those same settings. Both complete `ir.c` commands match after
normalizing only source/build paths and `BUSTER_INCLUDE_TESTS`. CI builds use
`-O2`, which was the source of the mistaken label; the earlier paired
comparisons still used matching configurations. The immutable earlier audit
and raw evidence are preserved, with this correction recorded separately.

Exact configuration and build commands, run in a detached worktree at the
source revision, with the same Clang 18.1.3 toolchain environment:

```sh
/path/to/existing/build/build generate --cc clang --linker DEFAULT -- \
  -DBUSTER_INCLUDE_TESTS=OFF -DBUSTER_UNITY_BUILD=OFF
CMAKE_BUILD_PARALLEL_LEVEL=1 /path/to/existing/build/build build \
  --config Release -t ide -- -j1
```

The [profile record](evidence/2026-09-11T211034Z/profile.json) preserves exact
absolute commands, full compiler flags, the macro probe, wrapper contents and
hashes. The [CMake cache](evidence/2026-09-11T211034Z/CMakeCache.txt.gz),
[configuration log](evidence/2026-09-11T211034Z/configure.log.gz),
[build log](evidence/2026-09-11T211034Z/build.log) and
[preprocessor output](evidence/2026-09-11T211034Z/preprocessor.log.gz) are retained.

## Output validation

The new compiler is 26,682,640 bytes, SHA-256
`3c4fa3079c08715dc207a7cf27ab42c428a5e205111ab0179f44f688183802ca`.
The fully tested compiler is 39,375,056 bytes, SHA-256
`1e54c62673883a7a67b5dd9ca93322cb006984ea150692ae415d293302958ada`.
Both hashes are unchanged after validation and measurement.

The exact frozen six workloads, all four allocator modes, and both optional
pass masks (OFF and ALL) compile with each compiler. All **96 invocations
succeed**, and all **48 paired object artifacts match byte for byte** across
build profiles. Source hashes are checked against the preceding corpus.
The [validation script](evidence/2026-09-11T211034Z/validate_outputs.py),
[results log](evidence/2026-09-11T211034Z/output-validation.log) and
[full commands, hashes, source metrics and invocation logs](evidence/2026-09-11T211034Z/output-validation.tar.gz)
are retained. This is output equivalence on this corpus; it does not assert
that a compiler with tests excluded ran the embedded suite. The complete
Debug suite, strict-warning compilation and opted-in self-hosting for this
unchanged source are recorded in the preceding audit.

## Same-binary OFF / ALL comparison

The existing native harness compares wrappers that invoke this same new
compiler with `-fno-canonical-fast` or `-fcanonical-fast`. The wrappers append
the flag only to `cc`. Corpus, seed 20260907, CPU 8, two warmups per variant,
and two rounds of 20 adjacent pairs match the earlier full-mode experiment.
All other task-owned builds, tests and benchmarks are idle. The guarded run
has no timing instrumentation or allocation diagnostics in the compiler.
Every one of the **1,920 timed observations** is retained.

The unchanged guard reports **zero confirmed substantial regressions,
21/24 inconclusive cases, exit 0**. Its wall threshold is both 15% and 2 ms;
its peak-RSS threshold is both 20% and 16 MiB, significant in both rounds
under the same family-wise test. This broad guard does not prove equivalence.

| Production-profile case | Paired wall-time estimate | Guard decision |
| --- | ---: | --- |
| Control flow / FAST | +0.88% | Inconclusive |
| Backend pressure / FAST | -2.48% | Inconclusive |

The other estimates are mixed; the complete 24-case table ranges from
-2.60% to +3.57%. See the [full summary](evidence/2026-09-11T211034Z/summary.md)
and [all raw samples, host records, inputs, commands, metrics and logs](evidence/2026-09-11T211034Z/run.tar.gz).
Archives omit generated object binaries while preserving their hashes and
sizes. No unfavorable case or sample is removed.

This separate supported profile does not show the same positive point
estimates as the previous test-bearing control-flow/FAST (+6.53%) and
backend-pressure/FAST (+6.77%) observations. It does not isolate the exact
cause: excluding embedded tests also changes binary layout, and cross-profile
time differences were not the paired comparison. This experiment supplements
the previous evidence and does not replace it.

**#40's total-time equivalence/default-adoption acceptance remains unresolved.**
The default stays OFF. This hosted quiet window lacks qualified-host A/A
calibration; small mixed estimates and inconclusive intervals cannot establish
improvement or equivalence. No further source changes or experiments are
performed for this series.
