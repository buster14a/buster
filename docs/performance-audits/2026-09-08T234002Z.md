`2026-09-08T234002Z` (Linux x86-64, Clang 22.1.8, GNU binutils 2.47; **Recover
APX, REX2 and EVEX broadcast encoding fixes (#190, #191, #192)**).

## Scope and provenance

Recover three completed correctness fixes onto main
`6288a2bc47414c91c16a39768a458e991c94518c`, preserving the current x86
metadata encoder as the only encoding authority:

- `50c51d55d3a097e03f036449ba3d170739e43a57`: APX EVEX mandatory-prefix,
  VVVV and V-prime fields (#190). Fixed-W promoted forms preserve their
  opcode-selecting prefix; variable-width NDD rows retain width semantics.
- `0285c4d1f28570d3456c1d19d0531e2412d56914`: variable scalar memory width
  also controls REX2.W (#192). Keep the existing MOV and unary-IMUL safeguards:
  their fixed-width metadata does not satisfy the generic variable-width gate.
- `ade1dc421ba9d0a062b503cc584482b091564302`: FULL/HALF broadcast disp8
  scales and HALF source broadcast counts (#191). The machine-fast path has
  no broadcast shapes and passes false to the shared address helper.

The original #191 audit remains unchanged at `2026-09-07T233029Z`. Its
historical results are separate from this recovery validation. Conversion
memory-width inference in the source adapter remains tracked by #280.

## Independent oracle checks

The 60 frozen APX and 338 frozen REX2 AT&T cases match fresh GNU as 2.47
output exactly (364 and 2,075 text bytes). The original base compiler fails
60 APX cases and 109 REX2 cases. Add an explicitly sized Intel spelling for
each REX2 fixture: all 338 Intel spellings independently produce the same
GNU-as bytes, including base/index extension, word/dword controls and LOCK/FS.

The broadcast matrix has 1,620 cases across five instruction families,
XMM/YMM/ZMM, broadcast/non-broadcast, 18 signed displacement values, and
three address forms. Its independent opcode/displacement recipe matches GNU
as byte-for-byte (14,268 text bytes). All 1,458 supported source cases match
the recovered Release and strict-sanitizer compilers (12,816 bytes). The
baseline FULL-tuple source corpus fails exact comparison. The unsupported
162 HALF non-broadcast source cases stay outside the source-path claim;
they retain direct and exact-query regression coverage. Fifteen wrong
broadcast counts must reject without publishing output.

APX instructions were assembled and compared, not executed on this host.
No generated-code runtime performance claim is made.

## Fresh local validation before rebase

The unchanged base passed `./build.sh test_self_host --config Release`,
including its complete canonical compiler compile, MIR-stack path and
byte-identical 40,025,152-byte stage1/stage2 fixed point, before edits.

Commands on the recovered tree:

```sh
./build.sh generate --cc clang --config Release
./build.sh build --config Release -t test_all -- -j2
build/build generate --cc clang --config Debug --sanitize --build-directory build-sanitize
build/build build --config Debug --build-directory build-sanitize -t test_all -- -j2
```

Release passed 900,908/900,908 assertions and 44/44 modules. Sanitized Debug
passed 896,629/896,629 assertions and 44/44 modules with no sanitizer
reports. The smaller sanitizer count follows existing fixture policy. A
separate process-level assembly check of all recovered source corpora also
passed with `UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1` and
`ASAN_OPTIONS=halt_on_error=1:detect_leaks=1`.

These are correctness and integration results, not comparative throughput
measurements. Concurrent independent builds were active on the host, so
build and test durations are not used as performance evidence. Post-rebase
self-host, mode-matrix and full-suite results belong in the PR validation
record at the submitted revision.

`tools/new_audit.py --check` reports one inherited problem: the historical
`2026-08-24T131821Z` file does not start with its ID and parenthetical. The
same content exists on the base; this recovery leaves it unchanged.
