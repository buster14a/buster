type Number = union
{
    signed_value: s32,
    unsigned_value: u32,
}

code main[export] : fn [cc(c)] (argument_count: u32, argv: &&u8, envp: &&u8) s32
{
    data number: Number = { .signed_value = 1 };
    return number.signed_value;
}
