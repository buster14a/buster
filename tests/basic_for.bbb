code main[export] : fn [cc(c)] (argument_count: u32, argv: &&u8, envp: &&u8) s32
{
    data total: s32 = 0;

    for (data i = 0 .. 2)
    {
        total += i;
    }

    for (data i: s32 = @reverse(0 .. 2))
    {
        total += i;
    }

    return total;
}
