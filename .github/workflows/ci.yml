# Temporary source-transport branch only; implementation PRs retain the full CI.
name: Buster CI
on: workflow_dispatch
permissions:
  contents: read
jobs:
  unused:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Source transport branch; no compiler changes.'
