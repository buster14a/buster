name: Temporary source transport branch
on: [workflow_dispatch]
permissions:
  contents: read
jobs:
  transport-only:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'This temporary branch is not an implementation PR; its snapshot workflow transports source only.'
