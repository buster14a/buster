# Temporary transport branch only; actual implementation branches retain full CI.
name: Buster CI
on: [push]
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
jobs:
  transport-only:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'No compiler changes in this transport branch.'
