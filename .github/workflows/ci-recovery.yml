name: Cancelled CI recovery

on:
  workflow_run:
    workflows: [Buster CI]
    types: [in_progress, completed]
  pull_request:
    paths:
      - '.github/workflows/ci-recovery.yml'
      - '.github/scripts/recover-ci.py'
      - 'tests/ci_recovery_test.py'
  push:
    branches: [main]
    paths:
      - '.github/workflows/ci-recovery.yml'
      - '.github/scripts/recover-ci.py'
      - 'tests/ci_recovery_test.py'

permissions: {}

# Serialize lifecycle deliveries for one Buster CI run so the trusted watcher
# finishes before any completion-recovery decision.
concurrency:
  group: ci-recovery-${{ github.event.workflow_run.id || github.run_id }}
  cancel-in-progress: false

jobs:
  test:
    if: ${{ github.server_url == 'https://github.com' && vars.GH_ACTIONS_CI_ENABLED == 'true' && github.event_name != 'workflow_run' }}
    runs-on: ubuntu-26.04
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - name: Test recovery decisions without network access
        run: python3 tests/ci_recovery_test.py

  watch-merge-group:
    if: >-
      ${{ github.server_url == 'https://github.com' &&
          vars.GH_ACTIONS_CI_ENABLED == 'true' &&
          github.event_name == 'workflow_run' &&
          github.event.action == 'in_progress' &&
          github.event.workflow_run.event == 'merge_group' &&
          github.event.workflow_run.head_repository.full_name == github.repository }}
    runs-on: ubuntu-26.04
    timeout-minutes: 305
    permissions:
      contents: read
      actions: write
    steps:
      # workflow_run executes this trusted default-branch revision, not the
      # merge-group candidate. Never execute candidate bytes or read artifacts.
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          repository: ${{ github.repository }}
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Cancel a doomed merge group on first CI job failure
        env:
          GH_TOKEN: ${{ github.token }}
        run: python3 .github/scripts/recover-ci.py watch

  recover:
    if: >-
      ${{ github.server_url == 'https://github.com' &&
          vars.GH_ACTIONS_CI_ENABLED == 'true' &&
          vars.GH_ACTIONS_CI_RECOVERY_ENABLED != 'false' &&
          github.event_name == 'workflow_run' &&
          github.event.action == 'completed' &&
          github.event.workflow_run.event != 'merge_group' &&
          github.event.workflow_run.run_attempt == 1 &&
          github.event.workflow_run.head_repository.full_name == github.repository &&
          (github.event.workflow_run.conclusion == 'cancelled' ||
           github.event.workflow_run.conclusion == 'failure') }}
    runs-on: ubuntu-26.04
    timeout-minutes: 5
    permissions:
      contents: read
      pull-requests: read
      actions: write
    steps:
      # workflow_run's github.sha is the trusted default-branch revision.
      # Never check out the completed run's head, read its artifacts, or run it.
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          repository: ${{ github.repository }}
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Retry an interrupted current PR once
        env:
          GH_TOKEN: ${{ github.token }}
        run: python3 .github/scripts/recover-ci.py
