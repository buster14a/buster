on: [push]
jobs:
  test:
    runs-on: zen5-linux
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Test
        shell: bash
        run: |
          set -euox pipefail
          ./generate.sh
          ./build.sh test_all
