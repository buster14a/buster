on: [push]

env:
  BUSTER_CI: 'ON'

jobs:
  test_controller:
    strategy:
      matrix:
        runner: [zen5-linux, x86_64-linux-sapphirerapids, aarch64-macos-m1]
    name: All [${{matrix.runner}}]
    uses: ./.forgejo/workflows/unix.yml
    with:
      runner: ${{ matrix.runner }}
