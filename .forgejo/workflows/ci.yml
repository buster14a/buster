on: [push]

env:
  BUSTER_CI: 'ON'

jobs:
  test_controller:
    strategy:
      matrix:
        runner: [zen5-linux, x86_64-linux-sapphirerapids, aarch64-macos-m1, x86_64-windows-znver5]
    name: All [${{matrix.runner}}]
    uses: ./.forgejo/workflows/unix.yml
    with:
      runner: ${{ matrix.runner }}
