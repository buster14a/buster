[Unit]
Description=Buster benchmark containment slice (reference values)

[Slice]
AllowedCPUs=2
MemoryMax=8G
MemorySwapMax=0
TasksMax=256

# Per-job services repeat these values and add RuntimeMaxSec. The worker verifies
# the leaf and every ancestor before allowing its fixed helper to run.
