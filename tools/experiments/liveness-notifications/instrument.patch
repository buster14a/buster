--- a/src/buster/lib/compiler/codegen/register_allocator_fast.c
+++ b/src/buster/lib/compiler/codegen/register_allocator_fast.c
@@ -1665,2 +1665,6 @@
 // themselves; this only widens them.
+#define BUSTER_LIVENESS_REPLAY 1
+#include <buster/lib/compiler/codegen/liveness_notification_probe_internal.h>
+#undef BUSTER_LIVENESS_REPLAY
+
 BUSTER_GLOBAL_LOCAL void machine_fast_close_live_ranges(Arena* arena, MachineFunction const* function, MachineFastPrepass const* prepass, u64 const* reads,
@@ -1668,2 +1672,3 @@
 {
+    LpEvidence lp_evidence = lp_begin(arena, function, prepass, reads, writes, words);
     u32 block_count = function->block_count;
@@ -1692,2 +1697,4 @@
         queued[block_index] = 0;
+        lp_add(&lp_evidence.actual, &lp_evidence.actual.pops, 1);
+        LpU64 lp_old_updates = lp_evidence.actual.in_updates;
         u64 const* block_reads = reads + (u64)block_index * words;
@@ -1698,3 +1705,10 @@
         {
+            LpU64 lp_previous_in = block_in[word];
             block_in[word] = block_reads[word] | (block_out[word] & ~block_writes[word]);
+            lp_add(&lp_evidence.actual, &lp_evidence.actual.transfer_words, 1);
+            lp_in_update(&lp_evidence.actual, lp_previous_in, block_in[word]);
+        }
+        if (lp_old_updates == lp_evidence.actual.in_updates)
+        {
+            lp_add(&lp_evidence.actual, &lp_evidence.actual.redundant_pops, 1);
         }
@@ -1704,2 +1718,3 @@
         {
+            lp_add(&lp_evidence.actual, &lp_evidence.actual.edge_visits, 1);
             u32 predecessor = prepass->predecessor_list[entry];
@@ -1709,2 +1724,3 @@
             {
+                lp_add(&lp_evidence.actual, &lp_evidence.actual.merge_words, 1);
                 u64 previous = predecessor_out[word];
@@ -1713,2 +1729,3 @@
                 {
+                    lp_out_update(&lp_evidence.actual, previous, merged);
                     predecessor_out[word] = merged;
@@ -1716,2 +1733,7 @@
                 }
+            }
+            if (predecessor_changed)
+            {
+                lp_add(&lp_evidence.actual, &lp_evidence.actual.notifications, 1);
+                lp_add(&lp_evidence.actual, &lp_evidence.actual.duplicates, queued[predecessor] != 0);
             }
@@ -1721,2 +1743,7 @@
                 queued[predecessor] = 1;
+                lp_add(&lp_evidence.actual, &lp_evidence.actual.pushes, 1);
+                if (work_count > lp_evidence.actual.max_pending)
+                {
+                    lp_evidence.actual.max_pending = work_count;
+                }
             }
@@ -1751,2 +1778,3 @@
     }
+    lp_finish(&lp_evidence, live_in, live_out);
 }
